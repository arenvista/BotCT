=== botc/enums.py ===
1) Purpose of the file
- Centralizes core game enums and a mutable status container for a Blood on the Clocktower–style engine.
- Provides role taxonomy (RoleClass) and concrete roles (RoleName) with metadata for categorization and display.
- Supplies utility properties and methods for game logic (e.g., reliability under poison/drunk, protection handling, role lookup by class).

2) Key classes/functions
- Status (dataclass)
  - Fields: poisoned, drunk, protected, alive, red_herring (all bool with sensible defaults).
  - is_reliable: True when not poisoned and not drunk; used to gate trustworthy info/actions.
  - is_protected: True when protected and not poisoned; poison nullifies protection.
  - toggle(attr_name): Dynamically flips a named boolean attribute; validates existence, else raises ValueError.
- Alignment (Enum)
  - GOOD, EVIL; __str__ returns title-cased name for user-friendly display.
- RoleClass (Enum)
  - GAMEMASTER, DEMONS, MINIONS, OUTSIDERS, TOWNSFOLK.
  - display_name and __str__: title-case with underscores replaced by spaces.
- RoleName (Enum)
  - Enumerates roles and attaches a role_class to each member via custom __new__.
  - Members cover GM plus Trouble Brewing–style Minions, Outsiders, Townsfolk, and Imp (Demon).
  - display_name and __str__: human-friendly names.
  - get_by_class(target_class): Returns a List[RoleName] filtered by RoleClass.

3) Important logic or algorithms
- Enum member augmentation: RoleName.__new__ sets both the integer value and an extra attribute (role_class), enabling per-member metadata without separate mappings.
- Reliability/protection rules encoded as properties:
  - is_reliable excludes poisoned/drunk states (mirrors game mechanics where such states corrupt info).
  - is_protected is suppressed by poison, modeling poison overriding protective effects.
- Dynamic state mutation: Status.toggle uses hasattr/getattr/setattr to flip flags by string, supporting flexible UI/command handling.
- Role lookup: get_by_class uses a concise list comprehension over the enum members for fast categorization.

4) Notable dependencies or patterns
- dataclasses for a lightweight, mutable state container with defaulted fields.
- enum.Enum with:
  - Custom __new__ to embed metadata per member.
  - __str__/display_name to decouple internal identifiers from UI text.
- Typing: List[RoleName] return type for clarity; forward reference used in annotation.
- Consistent display formatting pattern (replace underscores, title-case) across enums to standardize UI/printing.
- Stable integer values assigned to RoleName and RoleClass, useful for serialization or ordering.

=== botc/__init__.py ===
1. Purpose of the file
- Package initializer for the botc core engine. It provides a concise public API by re-exporting key types at the package level so users can do from botc import Player, Alignment, etc., and includes a package docstring.

2. Key classes/functions (with brief descriptions)
- Alignment (from .enums): Re-exported enum type representing player/team alignment.
- RoleClass (from .enums): Re-exported enum type categorizing roles (e.g., role archetypes/classes).
- RoleName (from .enums): Re-exported enum type enumerating role identifiers/names.
- Status (from .enums): Re-exported enum type for player or role status values.
- Player (from .player): Re-exported core class representing a player entity in the game.
Note: The actual implementations live in botc.enums and botc.player; this file only re-exports them.

3. Important logic or algorithms
- None. The file contains no business logic; it strictly re-exports symbols and provides a docstring.
- __all__ is commented out. Without __all__, from botc import * will import all public names defined in this module (here: Alignment, RoleClass, RoleName, Status, Player), but not automatically import submodules. Defining __all__ would make the intended public API explicit and stable.

4. Notable dependencies or patterns
- Uses intra-package relative imports to create a “barrel”/facade module that centralizes the public API.
- Establishes a clean namespace for consumers (botc.Alignment, botc.Player).
- No external library dependencies referenced here.
- Commented __all__ suggests an intention to control star-import semantics and clarify public API in the future.

=== botc/player.py ===
1) Purpose of the file
- Defines the Player entity for a Blood on the Clocktower-like game. Encapsulates identity (names and roles), alignment, status flags, role-driven behavior, and action execution within the game loop.

2) Key classes/functions
- Player
  - __init__(player_name, believed_role, registered_role, registered_alignment): Initializes core player state. Derives alignment if not provided, instantiates status, binds a role behavior based on believed_role, and sets vote tokens.
  - show_role(): Returns a simple greeting string identifying the player’s believed role.
  - _default_alignment(): Infers alignment from the registered role’s RoleClass (EVIL for DEMONS/MINIONS, otherwise GOOD).
  - take_action(game): Async entry point for a player’s nightly action; skips dead players except RAVENKEEPER, then delegates to the role behavior’s act().
  - _to_dict(): Serializes selected player fields (roles, alignment, and status flags) to a dict. Note: contains a duplicate "registered_role" key.

3) Important logic or algorithms
- Alignment inference: If no registered_alignment is supplied, it is computed from registered_role.role_class, mapping demons/minions to EVIL and all others to GOOD.
- Action gating: Only alive players act, except RAVENKEEPER who may act when dead (rule-specific exception). The actual action is delegated to the bound role behavior’s async act(self, game).
- Behavior selection: role_behavior is chosen via BEHAVIOR_MAP using the believed_role (not the registered_role), enabling a separation between what the player thinks they are and what they really are (useful for roles like Drunk/Poisoned or registration effects). A PassiveBehavior is used if no mapping is found.

4) Notable dependencies or patterns
- Strategy/behavior pattern: BEHAVIOR_MAP maps RoleName to a RoleBehavior; behaviors implement an async act(). PassiveBehavior provides a no-op fallback.
- Async integration: take_action is async to fit into an event-driven game loop managed by GameManager.
- Type hinting patterns: Uses from __future__ import annotations and TYPE_CHECKING imports to avoid runtime circular dependencies (GameManager, RoleBehavior).
- Enum-driven domain model: RoleName, RoleClass, and Alignment enums are central; RoleName appears to carry metadata (role_class). Status, though imported from enums, acts like a state container with attributes alive/poisoned/protected.
- Potential issues:
  - _to_dict has a duplicated "registered_role" key; the last one wins but it’s redundant and error-prone; also player_name and vote_token are omitted.
  - show_role prints the enum object directly; may produce verbose values unless RoleName.__str__ is customized (often .name or a display string is preferable).

=== botc/discord_manager/polling.py ===
1) Purpose of the file
- Orchestrates Discord-based polling for a Blood on the Clocktower game. Provides two async workflows:
  - A GM volunteer poll using Discord’s native Poll feature, selecting and updating the game’s Game Master.
  - A day execution vote using a custom dropdown view, tallying majority results and updating game state.

2) Key classes/functions
- class PollManager
  - __init__(game: GameManager)
    - Stores a reference to the game manager for state updates.
  - async run_gamemaster_poll(interaction, allowed_player_names) -> Optional[discord.Message]
    - Creates a public thread and posts a Discord Poll (“Yes” / “No / Skip”) asking who wants to be GM.
    - Waits up to ~4 minutes (or earlier if enough votes accumulated), ends the poll, filters “Yes” voters to allowed list (by username/global_name), randomly selects a GM, and updates/removes them from relevant game lists. Returns the poll message.
  - async run_execution_poll(interaction, allowed_player_ids) -> Optional[discord.Message]
    - Creates a public thread with an embed and attaches an ExecutionView dropdown (target selection).
    - Waits up to ~4 minutes or until all allowed voters have cast a vote; disables input, tallies votes (alive players + “Skip Vote”), requires strict majority (>50%) for execution, updates the executed player’s alive status and game.executed_player, and posts the outcome. Returns the poll message.

3) Important logic or algorithms
- Channel/type and ephemeral interaction handling:
  - Both methods require a TextChannel; they use interaction.response vs followup depending on whether the response is already sent.
- GM poll:
  - Poll creation via discord.Poll (duration set to 1 hour, but waiting loop only ~4 minutes).
  - Safe polling loop: sleep 5s intervals, refetch message, catch discord.HTTPException, early-exit when total votes across answers >= number of allowed players.
  - Poll termination: attempts poll_message.end_poll(); on failure, refetches the message.
  - Candidate filtering: normalizes allowed names (strip “@”, lowercase) and matches against user.name or user.global_name (lowercased).
  - Random selection: random.choice among filtered “Yes” voters; updates game.game_master and removes that player from game.players, game.players_alive, and game.player_names if present.
- Execution poll:
  - Uses a custom ExecutionView; votes stored in view.dropdown.votes as {voter_key: chosen_target}.
  - Wait loop ends when all allowed voters are present in votes or time expires; then disables the dropdown and edits the message.
  - Tally: builds a results dict for alive players plus “Skip Vote”; counts only votes from allowed voters; determines strict majority (> total_valid_votes / 2.0).
  - Outcomes: no votes => no execution; majority “Skip Vote” => no execution; otherwise marks player dead and records executed_player.
- Edge cases/nuances:
  - GM loop’s early-exit counts all poll votes (not only from allowed players), which may end the wait prematurely if outsiders vote.
  - Poll duration (1h) vs internal wait (4m) mismatch; code ends poll explicitly afterward.
  - Execution voting key semantics: comments indicate votes keyed by “username,” but parameter is named allowed_player_ids (strings). Logic assumes these keys match; mismatch would invalidate votes.
  - Majority uses strict “> 50%” (ties or exactly half do not pass).

4) Notable dependencies or patterns
- Discord.py features:
  - discord.Poll, PollAnswer.voters(), poll_message.end_poll(), discord.Embed, discord.ui style View (ExecutionView), public threads, Interaction followup/response.
  - discord.utils.get for poll answer retrieval.
- Async patterns:
  - Periodic polling with asyncio.sleep; robust fetch with HTTP exception handling; view disabling and message edit to lock input.
- GameManager integration:
  - Reads/updates: game.day_counter, game.game_master, game.players, game.players_alive, game.player_names, game.executed_player.
  - Methods: get_player_by_name, get_board_str.
- General patterns:
  - Input normalization (strip “@”, lowercase) and name/global_name matching.
  - Defensive error handling (channel checks, HTTP exceptions, missing players, null poll state).
  - Random selection for fairness when multiple GM volunteers exist.

=== botc/discord_manager/views.py ===
1) Purpose of the file
- Defines Discord UI components (Views, Selects, Buttons) to support a Blood on the Clocktower–style game flow:
  - Joining a lobby
  - Conducting an execution vote among alive players
  - A reusable generic dropdown selector for other prompts

2) Key classes/functions
- ExecutionDropdown (discord.ui.Select)
  - Builds a single-select dropdown with all alive players (labelled by player.player_name) plus a “Skip Vote” option.
  - Records selections in a local dict votes mapping Discord username -> chosen label.
  - Sends an ephemeral confirmation on each selection; allows changing selection until the poll is closed (component not disabled).
- ExecutionView (discord.ui.View)
  - Wraps an ExecutionDropdown and exposes it as a view (timeout=None).
- JoinLobbyView (discord.ui.View)
  - Persistent button “Join Game” (custom_id="join_game_btn") that adds the interacting user’s name to game.player_names and confirms ephemerally.
  - If already joined, responds with the current player list and a warning (see caveat about interaction responses below).
- DropdownView (discord.ui.View)
  - Generic dropdown supporting up to 25 options and N-way selection (min=1, max=max_selection).
  - On selection: stores selected_values, disables the component, edits the message to acknowledge, and calls self.stop() to unblock await view.wait() in calling code.
  - Intended for single-user flows tied to a specific message.

3) Important logic or algorithms
- Dynamic option construction:
  - ExecutionDropdown converts a list of alive player objects to SelectOptions by label=player.player_name and adds a “Skip Vote” sentinel.
- Vote collection strategy:
  - ExecutionDropdown stores votes by Discord username (interaction.user.name) to the chosen option label. This enables re-voting (overwriting) but:
    - Uses display names rather than unique IDs, which can collide if two users share the same name or if users rename themselves mid-game.
    - Stores state in-memory on the dropdown instance; not persisted across restarts or message re-creations.
- Interaction flow patterns:
  - Execution voting allows updates until the view is manually closed (component remains enabled).
  - The generic DropdownView locks after the first selection by disabling the menu and stopping the view; appropriate for single-user prompts but would block other users if used in a shared message.
- Timeouts and persistence:
  - ExecutionView and JoinLobbyView use timeout=None; the button in JoinLobbyView has a custom_id, making it suitable for persistent views across restarts if registered (bot.add_view). The select in ExecutionView lacks a custom_id, so it won’t persist across restarts even if the view is re-registered.
- Interaction response caveat in JoinLobbyView:
  - In the “already joined” branch, it calls interaction.response.send_message twice; Discord interactions allow only one initial response. Subsequent messages must use interaction.followup.send. As written, this will raise if executed.

4) Notable dependencies or patterns
- discord.py UI components (discord.ui.View, discord.ui.Select, discord.ui.Button, discord.SelectOption) and Interaction API (ephemeral replies, edit_message).
- TYPE_CHECKING gate and from __future__ import annotations to avoid runtime import cycles and enable forward type references (GameManager).
- Use of custom_id for persistent button; absence of custom_id for selects implies non-persistent components.
- Direct callback assignment (self.select.callback = self.select_callback) instead of decorator-based component definitions, enabling dynamic construction.
- Ephemeral confirmations are used liberally to avoid channel spam and provide private feedback.

=== botc/discord_manager/__init__.py ===
1) Purpose of the file
- Acts as the package initializer for botc.discord_manager, re-exporting key interfaces from submodules to provide a clean, convenient public API. This lets consumers import core components directly from botc.discord_manager rather than reaching into submodules.

2) Key classes/functions (with brief descriptions)
- BotManager (from .bot): Likely the central coordinator for Discord bot lifecycle and interactions (startup, event handling, command registration). Exposed as the primary entry point for bot control.
- JoinLobbyView (from .views): Likely a Discord UI component (e.g., a View with buttons/selects) for users to join a lobby or session.
- PollManager (from .polling): Likely manages creation, tracking, and aggregation of polls/votes within Discord.

3) Important logic or algorithms
- None within this file. It functions solely as an aggregator/re-export layer without implementing logic.

4) Notable dependencies or patterns
- Internal module dependencies: .bot, .views, .polling are imported and re-exposed.
- Pattern: Facade/public-API re-export. This simplifies imports (e.g., from botc.discord_manager import BotManager) and centralizes the package’s main touchpoints.
- Implicit export behavior: By binding these names at module level, they are included in star-imports (from botc.discord_manager import *) even without defining __all__, effectively defining the public surface.

=== botc/discord_manager/bot.py ===
1) Purpose of the file
- Defines a Discord bot manager that:
  - Configures intents and a command prefix.
  - Injects and exposes a GameManager instance.
  - Loads and registers a game-related Cog (GameCommands).
  - Performs fast, guild-scoped syncing of application commands for a test server.
  - Handles basic lifecycle logging on ready.

2) Key classes/functions (with brief descriptions)
- BotManager(commands.Bot)
  - __init__(game: GameManager): Sets up Discord intents (enables message_content and members), stores the provided GameManager, and initializes the bot with a "!" prefix.
  - setup_hook(): Asynchronous startup hook that:
    - Adds the GameCommands Cog (passing the bot and GameManager).
    - Loads environment variables via dotenv, determines a test guild ID (SERVER_ID), constructs a discord.Object for that guild, copies global app commands to the guild, and syncs them for fast iteration.
    - Logs that custom commands are hooked and synced.
  - on_ready(): Validates self.user is present and logs a readiness message.

- GameCommands (imported Cog): The command set for the game; receives the bot and GameManager. Not implemented here, but central to command registration.

3) Important logic or algorithms
- Use of setup_hook rather than on_ready for deterministic early setup:
  - Ensures Cogs are registered and application commands are synced before the bot is fully ready.
- Guild-scoped fast sync pattern for slash/app commands:
  - self.tree.copy_global_to(guild=TEST_SERVER) followed by self.tree.sync(guild=TEST_SERVER) to avoid global propagation delays during development.
- Configuration handling:
  - Loads SERVER_ID from a .env file (via dotenv) to select the test guild, with a hardcoded fallback ID.
- Dependency injection:
  - The GameManager is passed to the bot and Cog, centralizing game state and logic access.
- Intents configuration:
  - Enables privileged intents (message_content) and members to support prefix commands and member-related features if needed.

4) Notable dependencies or patterns
- discord.py:
  - commands.Bot for the bot framework, Cogs for modular commands, app command tree for slash/app commands and syncing.
- python-dotenv:
  - load_dotenv to source environment configuration at runtime.
- Typing patterns:
  - from __future__ import annotations and TYPE_CHECKING to avoid runtime imports and potential circular dependencies (type-only import of GameManager).
- Mixed command styles:
  - Maintains a message command prefix ("!") while primarily leveraging app command tree operations, suggesting hybrid command support.
- Operational notes/pitfalls:
  - SERVER_ID from os.getenv is a string; discord.Object expects an int. Consider casting (int(os.getenv("SERVER_ID"))) and handling None explicitly rather than using a bare except, which won’t catch missing/None values.

=== botc/encounters/enums.py ===
1) Purpose of the file
- Centralizes string identifiers for encounter event types used in the botc.encounters domain. Acts as a lightweight “enum” source of truth for categorizing or dispatching event behavior.

2) Key classes/functions
- No classes or functions; only module-level constants (string tokens):
  - NULL_EVENT_ENUM = "null_event": Placeholder/no-op event type.
  - NULL_EVENT_CHILD_ENUM = "null_event_child": Likely a “child” placeholder within an event tree/graph.
  - NULL_EVENT_ANTI_CHILD_ENUM = "null_event_anti_child": Likely the inverse branch of a child placeholder (e.g., negative/else path).
  - ONESHOT_EVENT_ENUM = "oneshot_event": Event intended to trigger only once.
  - FLAVOR_TEXT_EVENT_ENUM = "flavor_text_event": Narrative-only event without gameplay effects.

3) Important logic or algorithms
- None in this file. It defines constants only. The implied logic elsewhere would be string-based dispatch/routing of event handling using these tokens.

4) Notable dependencies or patterns
- No imports or external dependencies.
- Pattern: “Stringly-typed enums” — uses uppercase constants mapping to snake_case string codes rather than Python’s Enum class. This favors easy serialization (e.g., JSON) and loose coupling across modules but trades off type safety and IDE assistance.

=== botc/encounters/base.py ===
1) Purpose of the file
- Defines the core “Encounter” abstraction and lifecycle flags for a simple dependency-driven encounter system.
- Provides a registry via a class decorator to auto-instantiate and register encounters by name, with declared dependency and anti-dependency names.
- Offers utilities to recompute encounter availability based on resolved dependencies and to reset state (useful for tests).

2) Key classes/functions
- ENCOUNTER_MAP: Global dict[str, Encounter], mapping encounter names to their single instantiated Encounter objects.
- register_encounter(encounter_name, dependency_names, anti_dependency_names): Class decorator that instantiates the decorated class with those three parameters and registers the instance in ENCOUNTER_MAP. Returns the class (not the instance).
- class Encounter(ABC):
  - __init__(name, dependency_names, anti_dependency_names): Stores name and string lists of dependency names; initializes reference lists (dependencies, anti_dependencies), state flags (impossible, parents_resolved, resolved), and optional presentation fields (flavor_text, image).
  - async resolve(...): Abstract; subclasses implement how the encounter resolves (likely sets resolved=True and side effects).
  - __str__(): Returns a concise debug string with name, parents_resolved, impossible.
  - __bool__(): True if parents_resolved is True and impossible is False (enables “if encounter:” checks to mean “encounter is possible”).
- update_possibilities(): For every registered encounter, recomputes:
  - impossible: currently set True only if all anti_dependencies are resolved; False if any anti_dependency is not resolved or there are none.
  - parents_resolved: True only if all dependencies are resolved (vacuously True if no dependencies).
- reset_resolved(): Sets impossible=False, parents_resolved=False, resolved=False for all encounters (intended mainly for testing; call update_possibilities afterward to recompute availability).

3) Important logic or algorithms
- Availability model:
  - parents_resolved = all(dep.resolved for dep in dependencies) (empty list => True).
  - impossible is computed as all(anti.resolved for anti in anti_dependencies) or False if there are none.
  - Encounter truthiness (__bool__) is parents_resolved and not impossible.
- Notable inconsistency/bug: The class comment states “if any of these anti-dependencies resolve, this encounter becomes impossible,” but update_possibilities marks impossible=True only when all anti-dependencies have resolved. The intended logic likely should be impossible = any(anti.resolved for anti in anti_dependencies).
- Dependencies vs names: dependency_names/anti_dependency_names are stored, but this module does not wire them into dependencies/anti_dependencies object references; an external step must populate those lists.

4) Notable dependencies or patterns
- Pattern: Decorator-based registry with eager instantiation (one instance per decorated class, singleton-like via ENCOUNTER_MAP).
- Typing: from __future__ import annotations enables forward references; lists are annotated as List[Encounter].
- Async design: resolve is async, implying integration with an event loop; update/reset are synchronous and global.
- PIL: Optional Image.Image for encounter visuals.
- Minor issues: random is imported but unused; a comment typo (“impossible always takes precedence over impossible”).

=== botc/encounters/deck.py ===
1) Purpose of the file
- Defines a deck system for “encounters” in a game. It loads a decklist from packaged JSON, represents encounters as draw-able cards (with rarity via duplicate cards), enforces prerequisite/possibility constraints when drawing, and tracks resolved vs unresolved cards.

2) Key classes/functions
- EncounterCard
  - Thin wrapper holding a specific_encounter (Encounter). Multiple cards can reference the same Encounter to weight draw probability.
- Deck
  - from_json(json_file_name): Classmethod that loads a decklist from botc.decklists/<name>.json. Expects keys "encounter_name_list" and "count_list". Resolves names via ENCOUNTER_MAP and returns a Deck instance.
  - __init__(encounter_list, count_list): Builds the unresolved deck by creating count copies of EncounterCard per Encounter; initializes constraints via update_possibilities().
  - draw_card() -> EncounterCard: Filters unresolved cards to those whose encounter.parents_resolved is True and encounter.impossible is False; randomly selects one, marks its encounter as resolved, moves the card to resolved, calls update_possibilities(), and returns the card.
  - __str__(): Summarizes unresolved/resolved cards by encounter name with frequencies.
  - __len__(): Returns total number of cards (resolved + unresolved).
  - __bool__(): Returns False if no unresolved cards; otherwise True if any unresolved card has a non-null specific_encounter (effectively True if unresolved is non-empty).

3) Important logic or algorithms
- Eligibility filtering on draw: Only encounters whose prerequisites are satisfied (parents_resolved) and not marked impossible can be drawn. Selection is uniform among eligible cards; duplicate cards weight probabilities linearly.
- State propagation: Upon draw, encounter.resolved is set to True and update_possibilities() is called to recompute global feasibility (e.g., unlocking dependents or marking others impossible). Because multiple cards can reference the same Encounter instance, resolving one affects all of its duplicate cards.
- Data-driven deck construction: Deck composition is externalized in JSON, mapping encounter names to counts; ENCOUNTER_MAP translates names to Encounter instances.
- Edge cases/nuances:
  - If no cards are eligible (all fail the filter) but unresolved cards remain, random.choice([]) will raise an exception; __bool__ still returns True in this situation.
  - __len__ counts resolved cards too; not a measure of remaining drawables.
  - zip(encounter_list, count_list) silently truncates to the shorter list if lengths mismatch.
  - Deck mutates Encounter objects from ENCOUNTER_MAP (singletons), so multiple Decks/games in the same process would share state.

4) Notable dependencies or patterns
- Depends on botc.encounters.base:
  - Encounter: expected to expose name, resolved, impossible, parents_resolved.
  - update_possibilities(): side-effect function to recalculate encounter feasibility.
  - ENCOUNTER_MAP: name -> Encounter singleton mapping.
- Uses importlib.resources.files to load packaged JSON resources from botc.decklists.
- Randomness via random.choice; no seeding or reproducibility controls exposed.
- Pattern: duplicates-as-weighting (multiple cards per Encounter) instead of adjusting probabilities directly.
- Implements Python dunders for integration with idioms (truthiness, length, string representation).
- Type hints used for clarity; minimal object model (EncounterCard could be a dataclass but isn’t).

=== botc/encounters/__init__.py ===
1) Purpose of the file
- Package initializer that wires up encounter/event objects after all are instantiated.
- Resolves forward references: converts dependency names (strings) into actual object references on each encounter.
- Triggers a finalization step (update_possibilities) once dependencies are wired.
- Exposes Deck at the package level for convenient import (from botc.encounters import Deck).

2) Key classes/functions
- ENCOUNTER_MAP (from .base): Central registry mapping encounter names -> encounter instances. Each instance is expected to have:
  - dependency_names: list[str] of forward-referenced dependencies
  - anti_dependency_names: list[str] of forward-referenced anti-dependencies
  - dependencies: list[Encounter] to be populated with actual instances
  - anti_dependencies: list[Encounter] to be populated with actual instances
- update_possibilities (from .base): Recomputes derived state (e.g., which encounters are eligible/possible) after dependencies are resolved.
- Deck (from .deck): Public class representing an encounters deck (details defined in deck module).
- Submodules imported: base, simple, deck — their import side effects are relied upon to register/populate ENCOUNTER_MAP.

3) Important logic or algorithms
- Two-phase initialization (forward-reference resolution):
  - Phase 1 (in submodules): Encounter objects are created and registered in ENCOUNTER_MAP with only names for dependencies/anti-dependencies.
  - Phase 2 (here): For each encounter in ENCOUNTER_MAP, map each name in dependency_names and anti_dependency_names to the corresponding object in ENCOUNTER_MAP and append it to dependencies/anti_dependencies.
- Finalization: Calls update_possibilities() after all links are established so any logic depending on the dependency graph is consistent.
- Complexity: Linear in the number of dependency edges (O(N + M); N encounters, M total dependency + anti-dependency links).
- Executed at import time; assumes ENCOUNTER_MAP is fully populated by importing base and simple first.

4) Notable dependencies or patterns
- Delayed wiring/forward-reference pattern to avoid circular construction issues across modules.
- Module-level side effects: Importing the package performs graph wiring and state finalization once (per interpreter session, due to module caching).
- Import order dependencies: base and simple must register encounters before resolution (ensured by from . import base,deck,simple at the top).
- Potential caveats:
  - Lists are appended to without clearing; reloading the module could duplicate links.
  - Missing names in ENCOUNTER_MAP would raise KeyError (no validation/guarding here).
- Public API: Deck is re-exported, making botc.encounters a convenience entry point for consumers.

=== botc/encounters/simple.py ===
1) Purpose of the file
- Defines a set of minimal/test Encounter implementations to exercise the encounter registration system, dependency/anti-dependency handling, and the async resolve interface. Includes a simple flavor-text encounter for demonstrating output.

2) Key classes/functions
- register_encounter (decorator, imported from .base): Associates an Encounter subclass with an enum key and its dependency/anti-dependency lists, and registers it (likely in ENCOUNTER_MAP).
- Encounter (base class, imported from .base): Base for all encounters; expected to accept (name, dependency_names, anti_dependency_names) in __init__ and expose an async resolve method.
- NullEncounter (@register_encounter(NULL_EVENT_ENUM, [], [])): No-op encounter; resolve is an async no-op (returns None).
- NullEncounterChild (@register_encounter(NULL_EVENT_CHILD_ENUM, [NULL_EVENT_ENUM], [])): Depends on NullEncounter; resolve is a no-op; used to test dependency enforcement.
- NullEncounterAntiChild (@register_encounter(NULL_EVENT_ANTI_CHILD_ENUM, [], [NULL_EVENT_ENUM])): Mutually exclusive with NullEncounter; resolve is a no-op; tests anti-dependency enforcement.
- OneShotEncounter (@register_encounter(ONESHOT_EVENT_ENUM, [], [ONESHOT_EVENT_ENUM])): Self anti-dependency makes it selectable only once; resolve is a no-op; tests “one-shot” semantics.
- FlavorTextEncounter (@register_encounter(FLAVOR_TEXT_EVENT_ENUM, [], [])): Sets a flavor_text in __init__; resolve returns that string ("A crazy ecnounter just happened wow!!!").

3) Important logic or algorithms
- Declarative dependency modeling: Each encounter declares dependencies and anti-dependencies via the decorator, enabling an external scheduler/selector to filter valid encounters based on prior/selected events.
- One-shot pattern: Achieved by listing the encounter’s own enum in its anti-dependencies (prevents re-selection once it has occurred).
- Async resolution contract: resolve methods are async and accept flexible *args, **kwargs; return values vary (None vs. str), implying the engine must accommodate different result types.

4) Notable dependencies or patterns
- Registry/plugin pattern: Class decorators register encounters into a central map (ENCOUNTER_MAP), enabling discovery without manual wiring.
- Enum-driven keys: Encounter identities and constraints are expressed via enums imported from .enums (wildcard import used).
- Asynchronous design: All resolve methods are async, preparing for I/O or multi-step flows.
- Minimal test fixtures: Classes primarily serve as fixtures for testing dependency, anti-dependency, and registration behavior; only FlavorTextEncounter produces a visible result.

=== botc/core/distribution.py ===
1) Purpose
- Computes the number of roles (demons, outsiders, townsfolk, minions) to use in a Blood on the Clocktower game based on the number of players, using the official 5–15 player distribution.

2) Key classes/functions
- RoleDistributor
  - Attributes:
    - player_names: input list of player identifiers.
    - num_demons, num_outsiders, num_townsfolk, num_minions: computed counts per distribution.
  - Methods:
    - _calculate_role_counts(): Looks up the role count tuple from DISTRIBUTION_TABLE based on capped player count.

3) Important logic/algorithms
- DISTRIBUTION_TABLE: Hard-coded mapping from player count (5–15) to a tuple (demons, outsiders, townsfolk, minions) whose sum equals the player count.
- Player count handling: Uses min(len(player_names), 15) to cap at 15 players; directly indexes the table with that count.
  - Implication: Numbers below 5 or not in the table raise a KeyError (no lower bound clamp or validation).
- Computation occurs once at initialization; changing player_names afterward won’t update counts unless re-instantiated or method re-run manually.

4) Notable dependencies/patterns
- Dependencies: Only typing.List from the standard library.
- Patterns:
  - Table-driven configuration (explicit lookup over formula), mirroring official game setup.
  - Private helper method used during __init__ for derived attributes.
  - No validation/error messaging for unsupported player counts; relies on dict lookup.
  - Separation of concerns: provides counts only, not role assignment or randomness.

=== botc/core/__init__.py ===
1) Purpose of the file
- Acts as the package initializer for botc.core and defines its public API by re-exporting key classes. This lets consumers import core primitives via from botc.core import GameManager, RoleDistributor without knowing internal module structure.

2) Key classes/functions (with brief descriptions)
- GameManager (from .game): Exposed here for top-level access; likely orchestrates core game lifecycle/state (exact implementation resides in botc/core/game.py).
- RoleDistributor (from .distribution): Exposed here for top-level access; likely handles assigning/distributing roles (implementation in botc/core/distribution.py).

3) Important logic or algorithms
- None in this file. It only performs import-and-reexport; no computation, control flow, or algorithms.

4) Notable dependencies or patterns
- Intra-package relative imports from .game and .distribution.
- Re-export/facade pattern to present a simplified, stable public API and hide internal module layout.
- No __all__ defined; availability in the package namespace is achieved by binding names at import time.
- Import-time coupling: importing botc.core will also import the .game and .distribution modules (be mindful of potential side effects or heavy imports in those modules).

=== botc/core/game.py ===
1) Purpose of the file
- Central game orchestrator for a Blood on the Clocktower implementation integrated with Discord.
- Manages player setup and role assignment (including special role mechanics), game state (day/night counters, alive/dead, temporary statuses), and the main day/night loop.
- Coordinates Discord messaging, DM-based role reveals, and execution voting through bot/poll managers.

2) Key classes/functions (with brief descriptions)
- GameManager
  - __init__(player_names): Initializes game state, loads the default encounter deck, builds role distribution, assigns roles (if players provided), prepares Discord bot components (BotManager, PollManager, GameCommands), and sets up vote tables.
  - modify_information(message, choices, max_input): Proxies to a DM poll for the game master to make selections/edits.
  - reset_vote_table(): Initializes a nested voter→candidate→count table with zeros.
  - assign_roles(): Samples unique roles based on RoleDistributor counts; applies Baron’s outsider offset; implements Drunk’s “believed role” and Spy’s “registered role” mechanics; shuffles and creates Player objects with believed_role, registered_role, and registered_alignment.
  - resolve_temporary_conditions(): Resets per-cycle flags (protected, poisoned) on all players.
  - start_game(interaction): Runs initial role assignment and DMs roles; executes first-night wake order; then loops day/night: clears temporary states, runs execution poll (day), then executes non-first-night wake order (night).
  - message_roles_to_players(): DMs each player their role information via the command cog.
  - start_voting_phase(interaction): Runs the execution poll for currently alive players; handles Discord interaction response edge cases.
  - get_alive_neighbors(target_player): Returns the nearest alive left and right neighbors, wrapping the seating order.
  - get_player_by_role(target_role): Finds a player by their registered_role (not necessarily actual role).
  - get_player_by_name(player_name=""): Returns a Player by name; interactive prompt fallback for non-Discord contexts.
  - get_wake_order(is_first_night): Sorts players by role_behavior first-night or other-night priority; returns ordered list for action resolution.
  - get_board_str(): Produces a formatted debug board display including seat, name, alive/dead, role/alignment presentation, and status flags.

3) Important logic or algorithms
- Role assignment
  - Uses RoleDistributor to determine counts by class.
  - Samples roles without replacement from each class.
  - Baron effect: increases Outsiders by 2 and decreases Townsfolk by 2.
  - Drunk: assigned as an Outsider but “believes” they are a randomly chosen Townsfolk not already in play.
  - Spy: “registers” as a random unused good role (townsfolk/outsider) if available, distinct from the Drunk’s fake role.
  - Players receive:
    - believed_role: what the player thinks they are (Drunk twist).
    - registered_role: what abilities detect (Spy twist).
    - registered_alignment: set based on the actual role class (good for townsfolk/outsiders, evil for minions/demons).
- Wake order
  - Collects players with defined role_behavior priorities; sorts ascending by priority for first night vs other nights; executes actions asynchronously in that order.
- Game loop sequencing
  - Initial: assign roles, DM players, run first-night wake order.
  - Repeating cycle: increment night counter, clear temporary statuses, conduct daytime execution poll, increment day counter, run non-first-night wake order (night actions).
- Neighbor resolution
  - Walks left/right circularly to find nearest alive neighbors, handling all-dead-others edge case by returning the player themselves on that side.
- Voting
  - Poll-based execution via PollManager; maintains a structured vote_table initialized here (actual tallying likely handled by PollManager).

4) Notable dependencies or patterns
- Discord integration
  - Uses discord.py Interaction API; BotManager for bot lifecycle; PollManager for polls; GameCommands for DMs and interactive utilities (e.g., dmpoll).
  - Asynchronous flow (async/await) throughout player actions and Discord I/O.
  - Environment-driven token via dotenv (DISCORD_TOKEN).
- Game entities and enums
  - RoleName.get_by_class(RoleClass) drives role pools.
  - Player exposes status (alive/poisoned/protected), role_behavior priorities, show_role(), and async take_action(game).
  - RoleDistributor encapsulates player-count-based role distribution rules.
- Data modeling and patterns
  - Distinguishes believed_role vs registered_role vs registered_alignment to model BotC information/registration quirks.
  - Composition: GameManager composes bot/poll/cog managers and delegates UI concerns.
  - Event system stubbed: Deck.from_json("default") loaded; sample event draw and resolution code exists but is commented out.
- Notes/edge considerations
  - players_alive is a separate list from players and must be kept in sync externally when players die.
  - get_player_by_role searches by registered_role, which may differ from actual role (Spy/Drunk mechanics).
  - Mutable default argument for player_names in __init__ (List[str] = []) is a Python antipattern.
  - Night/day counter updates happen before/after actions in a way that may not align with human-facing “night number” expectations, depending on how they’re reported.

=== botc/behaviors/townsfolk.py ===
1) Purpose of the file
- Implements night/day behaviors for Townsfolk roles in a Blood on the Clocktower engine.
- Provides concrete Info, Action, Custom/Hybrid, and Passive role behaviors, wired to RoleName via a register_role decorator.
- Supplies a reusable player-filtering helper used across actions and prompts.

2) Key classes/functions (with brief descriptions)
- get_filtered_players(game, role_names_registered=None, role_names_believed=None, alignment=None, is_poisoned=None, is_drunk=None, is_alive=None, is_protected=None, excluded_players=None) -> List[Player]
  - Utility to derive a filtered list of players by alignment, status flags (poisoned/drunk/alive/protected), registered role(s), and exclusions. Note: role_names_believed currently filters against registered_role (same as role_names_registered), and excluded_players uses set difference (may reorder).

- WasherwomanBehavior (InfoRoleBehavior; first_night_priority=3)
  - Provides two names: one real Townsfolk and one decoy; reports “One of X and Y is the <role>.”
  - get_default_trusted/manual allow GM override of which townsfolk and decoy; dishonest path randomizes names and a Townsfolk role.

- LibrarianBehavior (InfoRoleBehavior; first_night_priority=4)
  - Mirrors Washerwoman for Outsiders; special-case: if 0 Outsiders, returns a one-item list that triggers “There are no Outsiders in play.”
  - Dishonest path randomizes two names and an Outsider role.

- ChefBehavior (InfoRoleBehavior; first_night_priority=6)
  - Counts adjacent pairs of evil players around the circular seating order and reports the number.
  - Dishonest path provides a plausible random number (0–min(num_evil, 2)); optional GM override.

- UndertakerBehavior (InfoRoleBehavior; other_night_priority=7)
  - If someone was executed today, reports the executed player’s role.
  - Dishonest path picks any non-actual role; optional GM override of the false role.

- MonkBehavior (ActionRoleBehavior; other_night_priority=2)
  - Prompts the player (or GM override) to select a living target; sets target.status.protected = True for the night.

- FortuneTellerBehavior (RoleBehavior; first_night_priority=8, other_night_priority=9)
  - Custom act() that asks the player for two targets and returns YES if a Demon or a “red herring” is among them (if player is reliable), else random YES/NO when drunk/poisoned.

- RavenkeeperBehavior (RoleBehavior; other_night_priority=6)
  - If killed_tonight matches the Ravenkeeper, lets them select any player to learn their role; returns actual role if reliable, otherwise a random incorrect role.

- Passive roles (PassiveBehavior): Virgin, Slayer, Soldier, Mayor
  - No night actions here; each is triggered by day-phase mechanics elsewhere (nominations, day command, kill resolution, end of execution).

- ROLES_DEMONS / ROLES_MINIONS / ROLES_OUTSIDERS / ROLES_TOWNSFOLK
  - Cached role-name collections by RoleClass, used for selection and checks.

3) Important logic or algorithms
- Info-role “4-step data pipeline”
  - get_default_trusted/get_manual_trusted produce true info (subject to GM override).
  - get_default_dishonest/get_manual_dishonest produce false info (for drunk/poisoned).
  - send_result formats and delivers the message. Base class likely chooses which path based on player.status.is_reliable.

- Chef adjacency counting
  - Iterates circularly over game.players; increments for i if players[i] and players[i+1] are both evil, giving the classic Chef count.

- Fortune Teller demon detection
  - Reliable: YES if any target is a Demon or marked red_herring; unreliable: random YES/NO.
  - Uses role-class membership (ROLES_DEMONS) and a status flag (red_herring).

- Undertaker and Ravenkeeper reliability gates
  - Reliable returns actual role; unreliable fabricates plausible false roles (never equal to target’s actual role).

- Monk protection
  - Sets a per-night status flag (protected) on the chosen target for downstream kill prevention.

- Librarian 0-Outsider handling
  - Encodes “no Outsiders in play” via a single-item players list and a special message.

4) Notable dependencies or patterns
- Decorator-based registration: @register_role(RoleName.X) binds each behavior to the role.
- Asynchronous I/O and GM/player interaction:
  - game.command_cog.dmdropdown(...) for player/GM prompts.
  - game.command_cog.send_direct_message(...) for results.
  - game.modify_information(...) for GM overrides of info roles.
- Engine integration points:
  - GameManager: players; executed_player; killed_tonight; game_master; get_player_by_name(...).
  - Player: registered_role; registered_alignment; status flags (alive, poisoned, drunk, protected, red_herring), and is_reliable gate.
- Scheduling via priorities:
  - first_night_priority and other_night_priority determine night order of actions/info.
- Role taxonomy via enums:
  - RoleName, Alignment, RoleClass; RoleName.get_by_class(...) partitions roles (e.g., demons/outsiders).
- Notes on filtering/utilities:
  - get_filtered_players uses set subtraction for exclusions (may change ordering vs seating).
  - role_names_believed parameter currently duplicates the registered-role filter (no separate “believed role” concept here).
- Randomization:
  - Used for decoys, dishonest info, and unreliable effects to simulate drunk/poisoned behavior.

=== botc/behaviors/base.py ===
1) Purpose of the file
- Defines the abstract behavior framework for all role types in the game’s night phase. It standardizes how roles act, supports optional Game Master (GM) intervention, and models different archetypes: information roles, action/targeting roles, and passive roles.
- Encapsulates the template flow for night actions, including handling “reliability” (e.g., drunk/poisoned states) and deciding between default automated behavior and manual GM overrides.

2) Key classes/functions (with brief descriptions)
- RoleBehavior (abstract)
  - first_night_priority, other_night_priority: Optional scheduling hints for the night order.
  - is_reliable: Base flag (commented as dynamic in practice); actual reliability is read from player.status.is_reliable.
  - check_gm_override(game, player) -> bool: Asks the GM (via DM dropdown) whether to bypass automated behavior and enter a manual flow. Returns True when the GM chooses to intervene (selects “No” to “Skip Manual Entry?”).
  - act(player, game): Abstract template to be implemented by subclasses.

- InfoRoleBehavior (abstract, for roles that receive info)
  - act: Orchestrates info generation and delivery:
    - Determines reliability from player.status.is_reliable.
    - Collects default data via get_default_trusted or get_default_dishonest.
    - If GM opts in, replaces/adjusts with get_manual_trusted or get_manual_dishonest (receives default_data to edit).
    - If any data is produced, calls send_result to deliver info to the player.
  - Abstract hooks: get_default_trusted, get_manual_trusted, get_default_dishonest, get_manual_dishonest, send_result.

- ActionRoleBehavior (abstract, for roles that target without receiving info)
  - act: Selects a target via either get_default_target or get_manual_target depending on GM override; if a target is chosen, calls execute_action.
  - Abstract hooks: get_default_target, get_manual_target, execute_action.

- PassiveBehavior
  - act: No-op; for roles that do nothing at night.

3) Important logic or algorithms
- GM manual override flow:
  - If no GM configured (game.game_master == ""), skip manual flow.
  - Prompt text dynamically reflects reliability: indicates Drunk/Poisoned when player.status.is_reliable is False.
  - The prompt asks “Skip Manual Entry?” with options Yes/No; returning True on “No” means GM will provide manual input or decisions.
- Reliability branching (InfoRoleBehavior):
  - Trusted vs dishonest data paths ensure correct information or misinformation, aligning with statuses like drunk/poisoned.
  - Manual methods receive default_data to allow adjustments rather than building info from scratch.
- Safety checks:
  - Only send results when night_data is non-empty.
  - Only execute actions when a target is resolved (non-None).
- Async orchestration:
  - All behaviors use async/await to integrate with the game’s asynchronous event loop and UI prompts.

4) Notable dependencies or patterns
- Dependencies:
  - GameManager: Provides GM identity and command_cog.dmdropdown UI for prompts.
  - Player: Supplies registered_role, player_name, and status.is_reliable.
  - command_cog.dmdropdown(user_name, message_text, options, max_selection): External UI interaction for GM choices.
  - Imported but unused: RoleName, register_role (likely used in other modules for role registration).
- Design patterns:
  - Template Method: act methods in archetypes define the high-level algorithm; subclasses override specific steps (e.g., how to get default/manual info/targets, how to execute actions).
  - Strategy-like subclassing: Concrete role classes plug in behavior for each abstract hook.
  - Inversion of control: Default generation first, then optional manual override merges/overrides data.
- Conventions:
  - Night priority attributes suggest an external scheduler orders role actions.
  - Reliability handled via player.status rather than the base is_reliable flag, enabling dynamic effects like poison/drunk states.

=== botc/behaviors/demons.py ===
1) Purpose of the file
- Defines night action behavior for demon roles, specifically the Imp, in a Blood on the Clocktower game implementation. Integrates with a Discord UI to collect the Imp’s target and resolves kills with protection checks, updating game and player state accordingly.

2) Key classes/functions
- ImpBehavior(RoleBehavior)
  - other_night_priority = 5: Determines execution order among roles on “other nights.”
  - async act(player, game): Prompts the Imp via a Discord DM dropdown to choose a target, resolves the kill considering protection, updates state (player.alive, game.killed_tonight), and logs outcome.
- register_role(RoleName.IMP): Decorator registering ImpBehavior under the IMP role for role-to-behavior lookup/dispatch.
- External calls used (from GameManager/Player, not defined here):
  - game.command_cog.dmdropdown(player_name, prompt, options, max_select): Collects a single selection via Discord DM.
  - game.get_player_by_name(name): Resolves a player object from a name.
  - Player attributes used: player_name, alive, protected, believed_role.
  - GameManager attributes used: players, killed_tonight.

3) Important logic or algorithms
- Target selection:
  - Builds a target list of alive players excluding the acting Imp (self-kill is disallowed by this implementation).
  - Uses an async Discord DM dropdown to enforce a single valid selection; if none, exits without action.
- Kill resolution:
  - If the selected target is not protected: sets target.alive = False and records game.killed_tonight = target.
  - If protected: no death; logs a protection message referencing the Monk.
- Side effects and state transitions:
  - Mutates player and game state directly (alive flag, killed_tonight).
  - Emits console prints for outcome and a “Put {player.believed_role} to sleep.” message, likely serving as night-phase logging.

4) Notable dependencies or patterns
- Asynchronous, UI-driven action: Uses an async Discord Cog method (dmdropdown) for player input instead of terminal I/O.
- Role registration pattern: Decorator-based plugin/registry design mapping RoleName.IMP to its behavior class.
- Scheduling via priority: other_night_priority suggests an action ordering system across roles.
- Type-hint ergonomics: from __future__ import annotations and TYPE_CHECKING to avoid runtime import costs/cycles while keeping rich typing.
- Game-state coupling: Direct reliance on Player attributes (alive, protected, believed_role) and GameManager interfaces (players list, killed_tonight, get_player_by_name, command_cog).

=== botc/behaviors/minions.py ===
1) Purpose of the file
- Defines and registers the in-game behaviors for minion roles (Poisoner, Spy, Scarlet Woman) in a Blood on the Clocktower engine.
- Specifies night action priorities and the asynchronous actions each role performs during the night phase.
- Supports dynamic role behavior switching (e.g., Scarlet Woman becoming the Imp).

2) Key classes/functions
- PoisonerBehavior (RoleBehavior)
  - first_night_priority = 1, other_night_priority = 1
  - act: DM-polls the Poisoner to select one alive player; sets target.status.poisoned = True; sends the current board state via DM.
- SpyBehavior (RoleBehavior)
  - first_night_priority = 2, other_night_priority = 3
  - act: Sends the current board state to the Spy via DM.
- ScarletWomanBehavior (RoleBehavior)
  - other_night_priority = 4
  - act: If the Imp is dead and there are at least 5 alive players, converts the Scarlet Woman into the Imp by updating the player’s registered_role, believed_role, registered_alignment, and role_behavior (pulled from BEHAVIOR_MAP).
- register_role (decorator; imported)
  - Registers each behavior class to a RoleName for lookup/dispatch.
- BEHAVIOR_MAP (imported)
  - Global registry mapping RoleName to behavior classes; used to swap behavior at runtime (e.g., Scarlet Woman → Imp).

3) Important logic or algorithms
- Night action ordering via first_night_priority and other_night_priority to schedule behaviors.
- Poisoner targeting flow:
  - game.command_cog.dmpoll prompts the Poisoner with a list of alive players (by name), validates non-None selection, and marks the chosen target as poisoned.
- Board revelation:
  - Spy (and Poisoner post-action) receive game.get_board_str() via DM to inform them of the current state.
- Scarlet Woman promotion:
  - Checks: Imp exists, Imp is dead, and alive player count ≥ 5.
  - Mutates the player’s role and alignment and replaces their behavior with the Imp’s behavior from BEHAVIOR_MAP (runtime behavior swapping).
  
4) Notable dependencies or patterns
- Asynchronous I/O via command_cog (Discord-like interface): dmpoll for selection UI and send_direct_message for feedback.
- GameManager integration: uses get_player_by_name, get_player_by_role, get_board_str, and player collections (players, players_alive).
- Player state model: relies on Player.status fields (alive, poisoned).
- Enums: RoleName and Alignment to strongly type roles and factions.
- TYPE_CHECKING imports to avoid runtime circular dependencies.
- Behavior registration pattern: @register_role(RoleName.X) ties role enums to behavior classes; BEHAVIOR_MAP enables dynamic dispatch and mutation.
- Notable quirks/oversights:
  - Poisoner prompt typo ("poision") and an incongruent debug print (“Put ... to sleep.”).
  - Scarlet Woman’s promotion creates a prompt string but does not send/announce it; no DM to inform the player of the role change.

=== botc/behaviors/__init__.py ===
1) Purpose of the file
- Acts as the central registry for mapping role names to their behavior implementations in the botc.behaviors package.
- Provides a decorator (register_role) to auto-register behavior classes to one or more role names at import time.
- Forces import of submodules (base, demons, minions, outsiders, townsfolk) to trigger registration side effects.

2) Key classes/functions
- BEHAVIOR_MAP (dict): Global mapping of role_name -> behavior_instance. Serves as the lookup table for role behaviors.
- register_role(*role_names): Decorator factory.
  - Instantiates the decorated class with no arguments (cls()).
  - Registers that single instance under each provided role name in BEHAVIOR_MAP.
  - Returns the original class (not the instance), enabling normal class usage elsewhere if needed.

3) Important logic or algorithms
- Import-time registration: Decorated classes are instantiated and registered when their modules are imported; the bottom-of-file imports ensure this happens when botc.behaviors is imported.
- One-instance-per-class across roles: If multiple role names share the same behavior, they all point to the same class instance (state is shared). This assumes behaviors are stateless or safe to share.
- Last-write-wins for collisions: If multiple classes register the same role name, the mapping is overwritten by whichever import occurs last (no conflict checking).
- Zero-arg constructor requirement: Behavior classes must be instantiable with no arguments for the decorator to work.
- Global mutable state: BEHAVIOR_MAP is modified at import time; there’s no thread-safety or unregistering mechanism.

4) Notable dependencies or patterns
- Patterns:
  - Decorator-based plugin/registry pattern with import side effects for auto-discovery.
  - Singleton-like reuse of behavior instances across multiple role names.
  - Package __init__ used to centralize registration and submodule loading.
- Dependencies:
  - Internal modules: .base, .demons, .minions, .outsiders, .townsfolk (expected to define and decorate behavior classes).
  - No external libraries; relies solely on Python’s import system and decorators.

=== botc/behaviors/outsiders.py ===
1) Purpose of the file
- Defines behavior for Outsider roles in the game (Blood on the Clocktower), currently implementing the Butler’s night action. Integrates role behavior into the game engine via a registration system and provides the async action executed during night phases.

2) Key classes/functions
- ButlerBehavior(RoleBehavior)
  - first_night_priority = 9; other_night_priority = 10: Execution order hooks used by the night-phase scheduler.
  - async def act(self, player: Player, game: GameManager): Randomly selects a “master” from game.players and sends the Butler a direct message naming that master.
- @register_role(RoleName.BUTLER): Decorator that registers ButlerBehavior as the handler for the Butler role within the role registry.

3) Important logic or algorithms
- Random master selection: Uses random.sample(game.players, 1)[0] to pick any player as the Butler’s master each night the action runs.
  - Implications/edge cases:
    - May select the Butler themself (no self-exclusion).
    - May select dead/ineligible players (no alive filter).
    - Uses sampling with k=1 where random.choice would suffice.
  - The chosen master is not persisted in state; only a DM is sent. Enforcing the Butler’s voting restriction would require storing this selection elsewhere in the game state or rules engine.

4) Notable dependencies or patterns
- Role registration pattern: @register_role ties a RoleName enum member to a RoleBehavior subclass for dynamic discovery/dispatch.
- Async, event-driven action: act is awaited by the game loop; priorities control scheduling relative to other roles.
- Messaging integration: Relies on game.command_cog.send_direct_message(player_name, message), suggesting a Discord/bot-style command layer.
- Type annotations and imports:
  - TYPE_CHECKING guard to avoid circular imports at runtime (Player, GameManager).
  - from __future__ import annotations to defer annotation evaluation.
  - Depends on botc.enums.RoleName and the RoleBehavior base class contract.

=== botc/discord_manager/cogs/__init__.py ===
I don’t have the contents of botc/discord_manager/cogs/__init__.py, so I can’t analyze the exact implementation. Below is a concise summary of what this file typically does in Discord.py projects, plus how I’d summarize it if it’s empty. If you share the file contents, I’ll provide a precise, file-specific summary.

1) Purpose of the file
- Most common: Package initializer for the cogs package, optionally centralizing cog discovery and registration with the Discord bot (e.g., an async setup that loads all sub-cog modules).
- If empty: Solely marks botc.discord_manager.cogs as a Python package; no runtime behavior by itself.

2) Key classes/functions (typical)
- async def setup(bot): Central entry point used by discord.py’s extension loader; iterates over submodules and calls each cog module’s setup/async setup to add cogs to the bot.
- __all__ or exported names: Optional list of cog module names to control which cogs are exposed/loaded.
- Dynamic import helpers: Utility code using importlib and pkgutil to auto-discover and import cog modules.

3) Important logic or algorithms (typical)
- Dynamic cog discovery: Scans the package (via pkgutil.iter_modules over __path__) to find non-package modules representing cogs.
- Safe loading pattern: Imports each module (importlib.import_module) and, if present, awaits module.setup(bot) to register the cog; often wrapped with try/except to log or skip failing cogs.
- Explicit whitelist/blacklist: May use __all__ or naming conventions to include/exclude modules.

4) Notable dependencies or patterns (typical)
- discord.py (discord.ext.commands): Uses Cog/extension setup conventions (setup/async setup).
- importlib/pkgutil: For dynamic discovery and loading.
- Logging: Optional, to report which cogs loaded or failed.

If the file is empty or only contains a bare __init__:
1) Purpose: Mark directory as a package.
2) Key classes/functions: None.
3) Important logic: None.
4) Dependencies/patterns: None beyond standard packaging.

Please paste the file’s content to get an exact, implementation-specific summary.

=== botc/discord_manager/cogs/game_commands.py ===
1) Purpose of the file
- Defines a Discord Cog (GameCommands) that orchestrates core game-management flows for a Blood on the Clocktower bot:
  - Lobby management (open lobby, list players)
  - Seating order configuration via mentions
  - Kicking off game start and GM selection via a poll
  - Sending private UI prompts to players via DMs (dropdowns and polls)
  - Utility command to print guild users to console

2) Key classes/functions (with brief descriptions)
- GameCommands(commands.Cog): Cog binding bot and game services
  - __init__(bot, game): Stores BotManager and GameManager references
  - set_seats(interaction, mentions): Admin slash command to lock seating order based on mentioned users. Validates roster and reorders self.game.players/self.game.player_names; shows board via game.get_board_str()
  - dmdropdown(user_name, message_text, options, max_selection) -> Optional[List[str]]:
    - Sends a Discord UI dropdown (custom DropdownView) to a user’s DMs; waits for selection or timeout; returns chosen values
  - dmpoll(user_name, poll_message, poll_options, max_selection) -> Optional[List[str]]:
    - Sends a Discord Poll in DMs (if ≤10 options and multiple choice allowed); otherwise falls back to dmdropdown
    - Polls for results by refetching the message periodically; enforces max_selection and retries if exceeded
  - send_direct_message(user_name, message) -> bool: DMs a user by name/global_name; returns success flag; logs errors
  - print_users(ctx): Prefix command (admin) that prints guild member info to terminal; acknowledges in channel
  - open_lobby(interaction): Admin slash command that resets lobby state and posts a JoinLobbyView for players to join
  - start_game(interaction): Admin slash command that closes lobby, runs GM poll via PollManager, and starts the game via GameManager.start_game()
  - display_players(interaction): Lists current players from self.game.players

3) Important logic or algorithms
- Seating order via mentions
  - Extracts mentioned user IDs with regex r'<@!?(\d+)>'
  - Validates the number of mentions matches current game players
  - Resolves users (cache first, then fetch) and maps to user.name to derive new order
  - Verifies the new order matches current roster by name, then reconstructs self.game.players accordingly
  - Potential pitfalls:
    - Roster matching compares case-insensitive current_names to case-sensitive new_order_names; may misreport mismatches
    - Compares by user.name only (not IDs), which can be non-unique or change
- DM dropdown flow
  - Enforces 25-option limit (Discord UI constraint)
  - Looks up user by exact match of name/global_name (lowercased) in bot.users; no fetch fallback here
  - Waits on view; on timeout, disables the select and edits message; returns None
- DM poll flow
  - Uses discord.Poll (duration 1 hour) with multiple based on max_selection > 1
  - Refresh loop fetches the message every 2 seconds to detect any votes; for multi-select, adds a 3-second buffer before finalizing
  - If selected_count > max_selection, ends poll, informs the user, and restarts the poll recursively
  - Gathers chosen answers by vote_count > 0 and ends the poll
  - Falls back to dropdown if >10 options or single-select requested
- Game start and lobby
  - open_lobby resets game.player_names and posts JoinLobbyView
  - start_game runs PollManager.run_gamemaster_poll over game.player_names, then GameManager.start_game()
  - Notable anomalies:
    - Debug code injects many Player instances with placeholder name "iiiii5184" for every townsfolk role
    - Minimum players check uses num_players < 0 (likely intended < 5), so it never blocks
- Utility/user lookup patterns and error handling
  - Name/global_name lookups rely on bot.users cache; set_seats has a fallback fetch_user by ID, but DM helpers don’t
  - Handles discord.Forbidden and discord.HTTPException on DMs; logs to console
  - Uses ephemeral responses for most admin interactions; however, print_users uses ephemeral=True on ctx.send (not supported for text commands)

4) Notable dependencies or patterns
- Discord API usage:
  - discord.ext.commands Cog pattern with both slash (app_commands) and legacy prefix commands
  - Discord components via custom views (JoinLobbyView, DropdownView); view.wait pattern and component disabling on timeout
  - Discord Polls API (discord.Poll, add_answer, end_poll) used in DMs with periodic refetch to observe state
- Internal dependencies:
  - GameManager for game state, get_board_str(), start_game()
  - PollManager for GM selection workflow
  - RoleName/RoleClass for role categorization (used in debug player injection)
- Patterns and caveats:
  - Heavy reliance on username/global_name string matching; IDs would be safer
  - Redundant imports (Player, RoleName, Alignment repeated)
  - Mixed command paradigms (slash vs prefix) with an ephemeral misuse on a prefix command
  - Asynchronous polling loop with timeouts ensures responsiveness but adds API churn (fetch_message every 2 seconds)

=== botc/enums.py ===
1) Purpose of the file
- Defines core enumerations and a Status model for a “Blood on the Clocktower” (BOTC) engine.
- Provides role taxonomy (role classes and specific roles), alignment, and player state (poisoned, drunk, protected, alive, etc.).
- Supplies convenience properties and helpers to compute gameplay-relevant conditions (reliability of info, voting eligibility, protection), and to group/filter roles.

2) Key classes/functions
- Status (dataclass)
  - Fields: poisoned, drunk, protected, alive, red_herring, vote_chips (default 1).
  - is_reliable: True if not poisoned and not drunk (their info can be trusted).
  - can_vote: True if alive or has at least 1 vote chip (models ghost votes).
  - is_protected: True if protected and not poisoned (poisoning negates protection).
  - toggle(attr_name): Dynamically flips a boolean field by name; raises ValueError if missing.
  - resolve_temporary_conditions(): Resets protected and poisoned to False (clears ephemeral effects).
- Alignment (Enum)
  - Values: GOOD, EVIL; __str__ returns title-cased name.
- RoleClass (Enum)
  - Values: GAMEMASTER, DEMONS, MINIONS, OUTSIDERS, TOWNSFOLK.
  - display_name and __str__: human-friendly names.
- RoleName (Enum)
  - Enumerates all Trouble Brewing roles plus GAMEMASTER.
  - Custom __new__(value, role_class): attaches role_class metadata to each member.
  - display_name and __str__: human-friendly names.
  - get_by_class(target_class): returns list of roles in a given RoleClass.

3) Important logic or algorithms
- Reliability logic: Information is reliable only if the player is neither poisoned nor drunk.
- Voting logic: A player can vote if alive OR if they have at least one vote chip (supports dead “ghost” votes).
- Protection logic: Protection is nullified by poisoning (protection only applies if not poisoned).
- Dynamic status toggling: Attribute-by-name toggle enables generic UI/commands to flip status flags.
- Temporary condition resolution: End-of-phase/day style reset for protected/poisoned while leaving persistent states (e.g., drunk, alive, red_herring, vote_chips) unchanged.
- Enum metadata pattern: RoleName members carry their RoleClass via a custom __new__ and can be filtered by class.

4) Notable dependencies or patterns
- Uses dataclasses for compact state modeling (Status).
- Extends Enum members with extra attributes (role_class) via custom __new__—a common Python pattern for metadata-enriched enums.
- Consistent human-readable display via display_name and __str__ across enums.
- Type hints (List, RoleClass) aid clarity and tooling.
- Potential caveats:
  - Status.toggle will coerce non-bool fields (e.g., vote_chips) to bool if used incorrectly.
  - can_vote returns True for any alive player even with 0 chips, reflecting BOTC rules but worth noting if chips are also used for living players in a UI.

=== botc/__init__.py ===
1) Purpose of the file
- Package initializer for the botc core engine. It re-exports key types so users can write from botc import Player, Alignment, RoleClass, RoleName, Status instead of importing from submodules.

2) Key classes/functions (re-exported)
- Alignment (Enum): Represents team/alignment categories (e.g., good/evil), based on naming.
- RoleClass (Enum): Categorizes roles (e.g., Townsfolk/Outsider/Minion/Demon/etc.), based on naming.
- RoleName (Enum): Enumerates specific role identities.
- Status (Enum): Player/status state indicators (e.g., alive/dead/poisoned), based on naming.
- Player: Core data model for a game participant.

3) Important logic or algorithms
- No algorithms or behavioral logic. The file strictly aggregates and re-exports symbols.
- __all__ is present but commented out. With it commented, from botc import * will export all public names currently in the module namespace (the re-exported symbols), rather than a curated list.
- The commented __all__ contains a stray empty string item, which would be undesirable if enabled.

4) Notable dependencies or patterns
- Uses relative imports from sibling modules (.enums, .player).
- Employs a “barrel”/facade module pattern to provide a flat, ergonomic public API surface at the package root.
- No third-party dependencies; only internal modules are referenced.
- Public API shaping intended via __all__, though currently inactive.

=== botc/player.py ===
1) Purpose of the file
- Defines the Player entity for a Blood on the Clocktower–style game. It encapsulates identity, role/alignment (both “believed” vs “registered”/actual), status flags, and delegates night/day actions to a role-specific behavior.

2) Key classes/functions
- Player
  - __init__(username, believed_role, registered_role, registered_alignment): Initializes player identity, determines alignment (uses provided alignment or infers from role class), initializes status, and selects a RoleBehavior from BEHAVIOR_MAP based on believed_role.
  - show_role(): Returns a simple string revealing the believed role to the player.
  - _default_alignment(): Infers Alignment from the registered_role’s RoleClass (Demons/Minions → EVIL, else GOOD).
  - take_action(game): Async entrypoint to perform the role’s action. If the player is dead, only allows action when the believed role is Ravenkeeper; otherwise delegates to role_behavior.act(self, game).
  - _to_dict(): Serializes selected player fields to a dict (role names, alignment, and status flags).

3) Important logic or algorithms
- Role behavior dispatch: Uses a strategy map (BEHAVIOR_MAP) keyed by believed_role to obtain a RoleBehavior, defaulting to a PassiveBehavior instance. This means actions are driven by what the player believes they are, not necessarily their actual role—important for modeling drunk/poisoned mechanics.
- Dead-action gate: Prevents dead players from acting except when the believed role is Ravenkeeper (a special-case role that acts upon death). This is a design choice tied to believed_role; if the intent is to model only real Ravenkeeper triggers, this should reference registered_role instead.
- Alignment inference: If no alignment is provided, it is derived from the role class (Demons/Minions → EVIL; others → GOOD), ensuring consistent defaults.
- Serialization quirk: _to_dict defines "registered_role" twice in the literal; in Python the latter overwrites the former (same value), making the first redundant. Also, show_role prints the Enum directly; unless RoleName.__str__ is customized, it may render as "RoleName.X" rather than a human-friendly label.
- Typing nuance: __init__ accepts registered_alignment: Alignment but falls back when falsy (e.g., None). The signature implies non-Optional, but runtime supports Optional behavior.

4) Notable dependencies or patterns
- Enums: Alignment, RoleClass, RoleName, Status encapsulate core game semantics and flags (alive/poisoned/protected).
- Strategy pattern for role behaviors: BEHAVIOR_MAP provides per-role behavior objects; PassiveBehavior is the no-op default. The map is expected to contain RoleBehavior instances (not classes), given PassiveBehavior() is used as the default.
- Async actions: role behaviors implement an async act(self, game) API, coordinated via Player.take_action.
- Type hint hygiene: from __future__ import annotations and TYPE_CHECKING are used to avoid runtime import cycles (forward refs to GameManager and RoleBehavior).
- Separation of concerns: Distinguishes believed vs registered roles to support deception/poisoning mechanics typical of BotC-like games.

=== botc/discord_manager/polling.py ===
1. Purpose of the file
- Orchestrates two Discord-based polls for a Blood on the Clocktower game:
  - A Game Master (GM) volunteer poll using Discord’s native Polls.
  - A day-phase execution vote using a custom dropdown UI in a thread.
- Integrates poll outcomes back into the game state (e.g., setting executed player, marking alive status).

2. Key classes/functions
- Class PollManager(game: GameManager)
  - run_gamemaster_poll(interaction) -> str
    - Creates a native Discord poll (“Yes”/“No”) asking for GM volunteers.
    - Waits for votes until a threshold or 120s, finalizes the poll, gathers “Yes” voters (non-bots), randomly selects a GM, announces, and returns the selected display name (or "" on failure/no volunteers).
  - run_execution_poll(interaction, allowed_player_ids: list[str]) -> Optional[discord.Message]
    - Validates a text channel, sends an ephemeral kickoff message.
    - Spawns a public thread, posts an embed with an ExecutionView dropdown (players alive).
    - Waits up to 4 minutes (polling every 5s) or stops early if all allowed players have voted.
    - Disables the dropdown, tallies votes (including a “Skip Vote” option), requires >50% majority to execute.
    - Updates game state (sets executed player, marks as dead) and reports results. Returns the poll message (or None on invalid channel).

3. Important logic or algorithms
- GM poll early termination:
  - Tracks unique voters via raw_poll_vote_add on the specific message using a set of user_ids.
  - Breaks when unique voter count reaches len(self.game.mgr_player.player_names) or times out at 120s, then explicitly finalizes with end_poll() (native polls require a minimum duration).
  - Selects a GM uniformly at random among “Yes” voters (excludes bots).
- Execution vote:
  - Uses a custom view that records votes as a mapping {voter_name: chosen_target}.
  - Early-exit condition when all allowed voters have cast votes; otherwise timeout after 4 minutes.
  - Tallies only votes from allowed voters and only for alive players plus “Skip Vote”.
  - Majority rule: executes only if a target has > 50% of valid votes; otherwise no execution. Handles explicit majority “Skip Vote.”
- Interaction lifecycle handling:
  - Uses interaction.response.is_done to choose between initial response vs followup to avoid double-respond errors.
- Game state updates:
  - On execution, looks up the player by username, sets status.alive = False, and records mgr_day.executed_player.

4. Notable dependencies or patterns
- Discord API features:
  - discord.Poll (question, duration), poll answers, PollAnswer.voters() async iterator.
  - Event waiting with client.wait_for('raw_poll_vote_add', check=..., timeout=...).
  - Threads (discord.Thread) and public_thread channels for vote isolation.
  - Embeds and a custom View (ExecutionView) with a dropdown component; expects attributes:
    - view.dropdown.votes: dict[str, str] of voter->target.
    - view.dropdown.disabled: toggle to lock voting.
- Internal dependencies:
  - GameManager (TYPE_CHECKING to avoid runtime import cycles). Uses:
    - mgr_player.player_names
    - get_players(filter), get_player(name)
    - mgr_day.executed_player, day_counter, get_board_str()
- Patterns and caveats:
  - Minimum native poll duration workaround by explicitly ending the poll early via end_poll().
  - Only listens to raw_poll_vote_add (does not handle vote removals/changes for early-termination counting).
  - allowed_player_ids is compared to voter_name strings; naming suggests IDs but usage implies usernames.
  - Messaging inconsistencies: text says “Ends when 3 people vote!” but threshold equals number of players; day number not interpolated in some messages/thread names.
  - Returns display_name for GM (not ID), which may complicate downstream identification.

=== botc/discord_manager/views.py ===
1) Purpose of the file
- Defines Discord UI components (Views, Selects, Buttons) to support user interactions in a Blood on the Clocktower (BOTC) Discord bot, including joining a lobby, running an execution vote, and generic dropdown selections.

2) Key classes/functions
- ExecutionDropdown (discord.ui.Select)
  - Builds a dropdown of alive players (expects objects with player_name) plus a “Skip Vote” option.
  - Tracks votes in-memory via self.votes mapping {username: chosen_option}.
  - On selection, stores/overwrites the user’s vote and sends an ephemeral confirmation.

- ExecutionView (discord.ui.View)
  - Container for ExecutionDropdown with no timeout (intended to stay active indefinitely while the bot runs).

- JoinLobbyView (discord.ui.View)
  - Holds a “Join Game” button (custom_id="join_game_btn").
  - On click, adds the interacting user’s name to game.mgr_player.player_names and replies ephemerally with status.

- DropdownView (discord.ui.View)
  - Generic dropdown component: accepts options (max 25) and a max_selection.
  - Stores selected values, disables the dropdown post-selection, edits the original message to confirm, and calls stop() so await view.wait() can resume.

3) Important logic or algorithms
- Dynamic option generation:
  - ExecutionDropdown composes SelectOptions from alive players + a “Skip Vote” option with emojis.
  - DropdownView caps options at 25 to respect Discord limits.

- Vote collection:
  - ExecutionDropdown stores one vote per username; re-selections overwrite prior entries (simple dictionary state in memory, not persisted).

- Interaction flow:
  - Uses ephemeral feedback (send_message(ephemeral=True)) for user confirmations.
  - DropdownView disables the menu after selection and edits the original message (edit_message), then stops the view to unblock awaiting code.

- View lifetime and persistence:
  - ExecutionView and JoinLobbyView use timeout=None; they remain active while the process runs.
  - JoinLobbyView’s button has a custom_id, enabling potential persistent view usage across restarts if registered on startup.
  - ExecutionDropdown lacks a custom_id; it won’t be persistent across restarts.

- Constraints and UX:
  - DropdownView forces at least one selection (min_values=1); ExecutionDropdown enforces single choice (max_values=1).
  - ExecutionDropdown identifies voters by interaction.user.name (not ID), risking collisions or changes if usernames update.

- Notable pitfalls:
  - JoinLobbyView else-branch calls interaction.response.send_message twice, which will error (an interaction can only have one initial response; subsequent messages must use interaction.followup.send or be combined).
  - Player identity comparison is case-/name-based; consider using interaction.user.id for uniqueness.
  - Votes and lobby membership are maintained in-process only; no persistence or synchronization beyond the current instance.

4) Notable dependencies or patterns
- discord.py 2.x UI framework:
  - discord.ui.View, discord.ui.Select, discord.ui.button decorator, discord.SelectOption, Interaction response/edit semantics, custom_id usage for persistent components.
- TYPE_CHECKING guard:
  - Imports GameManager only for typing to avoid runtime dependency/import cycles.
- GameManager integration expectation:
  - Assumes game.mgr_player.player_names is a mutable list of player identifiers (strings).
- Callback binding styles:
  - Uses both decorator-based (button) and programmatic assignment (select.callback) patterns.

=== botc/discord_manager/__init__.py ===
1) Purpose of the file
- Acts as the package initializer and public API surface for botc.discord_manager.
- Re-exports key components from internal submodules so consumers can import them directly from botc.discord_manager without knowing the internal structure.

2) Key classes/functions (with brief descriptions)
- BotManager (from .bot): Likely the central coordinator for Discord bot lifecycle and behavior (startup/shutdown, command/event registration). Actual behavior is defined in bot.py.
- JoinLobbyView (from .views): Likely a Discord UI/view component for joining a lobby (e.g., buttons/select menus). Implementation resides in views.py.
- PollManager (from .polling): Likely manages creation, tracking, and results of polls/interactions. Implementation resides in polling.py.

3) Important logic or algorithms
- None in this file. It only re-exports symbols; no business logic or algorithms are implemented here.
- Note: Importing this module will import the submodules (.bot, .views, .polling), so any import-time side effects in those files (e.g., logger setup, global registrations) will occur.

4) Notable dependencies or patterns
- Pattern: Public API aggregation via relative imports, flattening the import path (from botc.discord_manager import BotManager, JoinLobbyView, PollManager).
- Encapsulation: Shields consumers from internal module layout changes while presenting stable names.
- No explicit __all__; exported names are defined implicitly by the imported symbols.
- Modular structure suggested by submodule names: separation of concerns between bot management, UI/views, and polling features.

=== botc/discord_manager/bot.py ===
1) Purpose of the file
- Defines a Discord bot manager class that configures intents, loads a game-related command Cog, and synchronizes application commands to a test guild, with environment-driven configuration for token and server ID.

2) Key classes/functions
- class BotManager(commands.Bot)
  - __init__(game: GameManager): 
    - Sets Discord Intents (enables message_content and members).
    - Stores a reference to the GameManager.
    - Reads DISCORD_TOKEN from environment into self.token.
    - Initializes the base Bot with command_prefix="!".
  - async setup_hook():
    - Adds the GameCommands Cog (constructed with the bot and GameManager).
    - Resolves a TEST_SERVER guild from SERVER_ID env var (with fallback ID).
    - Copies global application commands to the test guild and syncs only that guild for fast iteration.
  - async on_ready():
    - Verifies self.user is not None and logs a ready message.

3) Important logic or algorithms
- Early environment loading: load_dotenv() at module import ensures env vars are available before BotManager initialization.
- Intents configuration: explicitly enables privileged intents (message_content and members) required for reading message text and member events; these must be enabled in the Discord Developer Portal.
- Command registration flow:
  - Cog-based command organization (GameCommands) is added in setup_hook, the recommended async lifecycle hook for discord.py 2.x.
  - Uses app command tree operations:
    - tree.copy_global_to(guild=...) duplicates global commands into the specified test guild.
    - tree.sync(guild=...) performs a fast guild-scoped sync (quick propagation for testing).
- Guild ID handling:
  - Reads SERVER_ID via os.getenv; if missing or invalid, falls back to a hardcoded guild ID.
  - Uses discord.Object(id=...) to reference a guild by ID without fetching it.
- Token handling:
  - Stores the bot token from DISCORD_TOKEN into self.token for external startup code to use (e.g., bot.run(bot.token)).

4) Notable dependencies or patterns
- Libraries:
  - discord.py (discord, discord.ext.commands) for bot, intents, cogs, and app command tree management.
  - python-dotenv (load_dotenv) to source environment variables from a .env file.
  - os.getenv for configuration (DISCORD_TOKEN, SERVER_ID).
- Design/patterns:
  - Cog pattern for modular command registration (GameCommands).
  - TYPE_CHECKING gate to avoid runtime import of GameManager and prevent circular dependencies, while preserving type hints.
  - Asynchronous startup via setup_hook for reliable, pre-ready initialization and slash-command sync.
  - Environment-driven configuration with a safe fallback for SERVER_ID; token retrieval assumes presence of DISCORD_TOKEN (no explicit None check here).

=== botc/encounters/enums.py ===
1) Purpose of the file
- Centralizes enum-like string constants that label encounter/event types within the encounters subsystem, providing a single source of truth to avoid magic strings and typos across the codebase.

2) Key classes/functions
- None. The module defines only module-level constants:
  - NULL_EVENT_ENUM = "null_event" — sentinel/no-op event type.
  - NULL_EVENT_CHILD_ENUM = "null_event_child" — variant of the null event, likely used where events have parent/child relationships.
  - NULL_EVENT_ANTI_CHILD_ENUM = "null_event_anti_child" — complementary/inverse variant to the child null event (by naming).
  - ONESHOT_EVENT_ENUM = "oneshot_event" — event intended to trigger once.
  - FLAVOR_TEXT_EVENT_ENUM = "flavor_text_event" — narrative-only event without mechanical effects.

3) Important logic or algorithms
- None. The file contains no functions, classes, or control flow—only constant assignments.

4) Notable dependencies or patterns
- No imports; no external dependencies.
- Pattern: use of uppercase module-level constants as enum stand-ins (string literals), suitable for serialization, dispatch keys, or cross-module identifiers.
- Naming suggests an event hierarchy (child/anti_child) and lifecycle semantics (oneshot, flavor text).
- Potential improvement: replace with Python’s enum.Enum for type safety, IDE support, and prevention of invalid values, while preserving the same string values for serialization if needed.

=== botc/encounters/base.py ===
1) Purpose of the file
- Defines the core abstraction and lifecycle for “encounters” with dependency and anti-dependency relationships.
- Provides a global registry and utilities to register encounters, track their resolution state, and compute which encounters are currently possible/eligible.

2) Key classes/functions
- ENCOUNTER_MAP: Global dict mapping encounter_name -> Encounter instance (singletons created at import/decoration time).

- register_encounter(encounter_name, dependency_names, anti_dependency_names):
  - Decorator that instantiates the decorated class with the given name/dependency names and registers the instance in ENCOUNTER_MAP.
  - Enforces a one-instance-per-encounter pattern and expects the class __init__(name, dependency_names, anti_dependency_names).

- class Encounter(ABC):
  - Fields:
    - name: str
    - dependency_names, anti_dependency_names: List[str] of symbolic names to be resolved into object references later.
    - dependencies, anti_dependencies: List[Encounter] to be populated (not wired up in this file).
    - impossible: bool (encounter cannot happen)
    - parents_resolved: bool (all dependencies have resolved)
    - resolved: bool (this encounter has occurred)
    - flavor_text: str, image: PIL.Image.Image (UI-facing metadata)
  - Methods:
    - async resolve(...): abstract; subclasses implement the encounter and should set self.resolved = True when done.
    - __str__: returns a compact status string.
    - __bool__: returns parents_resolved and not impossible, allowing if encounter: ... checks for eligibility.

- update_possibilities():
  - Recomputes impossible and parents_resolved for all registered encounters based on their dependencies’ resolved flags.

- reset_resolved():
  - Resets impossible, parents_resolved, and resolved to False for all registered encounters (useful for tests/restarts).

3) Important logic or algorithms
- Dependency satisfaction:
  - parents_resolved is True only if all encounters in dependencies have resolved. If there are no dependencies, it remains True.
- Anti-dependency handling:
  - Current logic marks an encounter as impossible only if all anti_dependencies have resolved; if any anti_dependency is not resolved (or the list is empty), impossible is False.
  - This contradicts the class comment that says “if any of these encounters resolve, then this encounter becomes impossible,” suggesting a likely bug or mismatch in intent (should probably be “any” instead of “all”).
- Truthiness semantics:
  - Encounter truthiness encodes eligibility: an encounter is truthy when all deps are done and it is not impossible.
- Global-state update:
  - After changing any encounter.resolved, update_possibilities must be invoked to propagate availability/impossibility across the graph.

4) Notable dependencies or patterns
- Registry via decorator:
  - Decorator instantiates and registers singletons at import time (service-locator/singleton registry). The decorated class is returned unchanged; instances are accessed through ENCOUNTER_MAP.
- Asynchronous resolution:
  - resolve is async to support I/O, polling, or event-driven flows.
- Type hints and UI integration:
  - Uses from __future__ import annotations for forward references; PIL.Image.Image for optional encounter visualization.
- External wiring expected:
  - This module does not convert dependency_names to actual dependencies; another part of the system must populate Encounter.dependencies and Encounter.anti_dependencies.
- Minor notes:
  - random is imported but unused (cleanup candidate).
  - Variable naming parent_encounter is reused for anti-dependencies, which can be confusing.
  - Not thread-safe; relies on a global mutable registry.

=== botc/encounters/deck.py ===
1) Purpose of the file
- Defines a card/deck system for encounters. A Deck holds multiple EncounterCard instances (each wrapping an Encounter) to model rarity via card counts rather than probabilities.
- Supports loading a deck configuration from a packaged JSON decklist, drawing eligible encounters based on dependency constraints, and tracking resolved vs unresolved cards.

2) Key classes/functions
- EncounterCard
  - Lightweight wrapper around a specific Encounter instance (specific_encounter). Multiple cards can reference the same Encounter to adjust frequency.
- Deck
  - from_json(json_file_name): Classmethod that loads "count_list" and "encounter_name_list" from a JSON file in the botc.decklists package, maps names via ENCOUNTER_MAP to Encounter objects, and constructs a Deck.
  - __init__(encounter_list, count_list): Builds unresolved_encounter_card_list by repeating EncounterCard(encounter) count times for each pair; initializes resolved list; calls update_possibilities().
  - draw_card(): Filters unresolved cards to those whose Encounter satisfies parents_resolved and not impossible, randomly selects one, marks its Encounter as resolved, moves it to resolved list, calls update_possibilities(), and returns the card.
  - __str__(): Returns a frequency summary of unresolved and resolved cards by Encounter name.
  - __len__(): Returns total number of cards (unresolved + resolved), i.e., original deck size, not remaining.
  - __bool__(): True iff there is at least one unresolved card; does not check whether any are currently drawable.

3) Important logic or algorithms
- Rarity via duplication: Encounter frequencies are controlled by duplicating EncounterCard entries for the same Encounter (not by per-Encounter probability fields).
- Eligibility gating on draw: Only cards whose Encounter.parents_resolved is True and Encounter.impossible is False are considered; this enforces inter-encounter dependency constraints.
- State propagation: After deck creation and after each draw, update_possibilities() is called, presumably to recompute which encounters are impossible/eligible based on the current resolved set.
- Shared Encounter instances: Multiple cards reference the same Encounter object; setting resolved=True on draw mutates the shared instance, affecting all other cards for that Encounter (and potentially global state if ENCOUNTER_MAP provides singletons).
- JSON decklist contract: Expects keys "encounter_name_list" and "count_list"; names are resolved through ENCOUNTER_MAP; zip(encounter_list, count_list) silently truncates if lists are mismatched; negative/zero counts yield no cards for that encounter.

4) Notable dependencies or patterns
- Dependencies:
  - Encounter, update_possibilities, ENCOUNTER_MAP from .base (Encounter must expose name, parents_resolved, impossible, resolved).
  - importlib.resources.files to load packaged decklist JSON from botc.decklists.
  - random.choice for selection among eligible indices (raises on empty list if no eligible cards).
- Patterns/observations:
  - Global/shared-state pattern: update_possibilities() likely operates on shared Encounter state; Deck mutates Encounter.resolved directly.
  - Potential edge cases:
    - draw_card() will fail if no unresolved card is currently eligible (empty indices).
    - __bool__ returns True if any unresolved card exists, not if any is drawable under current constraints.
    - __len__ returns total deck size, which may be surprising if callers expect remaining count.
    - No validation that encounter_name_list/count_list lengths match or that counts are positive.
  - Simple O(n) filtering per draw via list comprehension over unresolved cards, acceptable for small decks.

=== botc/encounters/__init__.py ===
1) Purpose of the file
- Acts as the package initializer that wires together encounter/event objects after all are instantiated.
- Resolves name-based dependency references into actual object references in a shared registry (ENCOUNTER_MAP).
- Triggers a post-processing step (update_possibilities) once dependencies are bound.
- Exposes Deck as the primary public class and re-exports core registry/utilities from base.

2) Key classes/functions (with brief descriptions)
- ENCOUNTER_MAP (from .base): A global registry mapping encounter/event names to their instantiated objects.
- update_possibilities (from .base): Post-processing function likely computing/refreshing derived state (e.g., valid encounters, availability, or graph-based properties) after dependency wiring.
- Deck (from .deck): Public-facing class to build/use a deck composed of encounters; presumably depends on the resolved registry and computed possibilities.
- Encounter object interface (implied via ENCOUNTER_MAP values): Each object has
  - dependency_names: list of names (strings) of prerequisite encounters.
  - anti_dependency_names: list of names of mutually exclusive or blocking encounters.
  - dependencies: list to be populated with actual encounter objects.
  - anti_dependencies: list to be populated with actual encounter objects.

3) Important logic or algorithms
- Two-phase dependency resolution:
  - Phase 1 (on class instantiation, elsewhere): Encounters are created with name-based references only (dependency_names/anti_dependency_names) and registered in ENCOUNTER_MAP.
  - Phase 2 (here, at import time): For every encounter in ENCOUNTER_MAP, convert each name in dependency_names/anti_dependency_names to an object reference by looking up ENCOUNTER_MAP[name] and appending it to dependencies/anti_dependencies.
- Post-resolution update:
  - After wiring, update_possibilities() is called to recompute any derived constraints/availability that rely on fully resolved dependency graphs.

4) Notable dependencies or patterns
- Registry pattern: Central ENCOUNTER_MAP serves as the single source of truth for encounter instances and enables cross-references.
- Late binding / forward reference resolution: Name-based references allow encounters to reference others that may not yet exist at construction time; actual object links are resolved afterward.
- Side-effectful imports: Importing .simple likely instantiates and registers encounters; this module’s import-time code mutates encounter objects and global state. The order (import base/simple first, resolve, then import Deck) suggests Deck may rely on a fully wired registry.
- No error handling/deduping: Assumes all referenced names exist in ENCOUNTER_MAP; missing names would raise KeyError. Appends without clearing or deduplication—re-importing/reloading could duplicate entries if not guarded elsewhere.
- Graph construction: The code builds adjacency lists (dependencies and anti_dependencies), implying later algorithms (e.g., validation, topological checks, availability filtering) operate on this graph structure within update_possibilities or Deck.

=== botc/encounters/simple.py ===
1) Purpose of the file
- Defines a small set of minimal “encounter” classes used as test/dummy events to validate the encounter system’s registration, dependency, anti-dependency, and async resolution mechanics.
- Demonstrates decorator-based registration of encounters keyed by enum identifiers, including one-shot behavior and simple output.

2) Key classes/functions
- register_encounter(enum_name, dependency_names, anti_dependency_names) [imported]: Decorator that associates an Encounter subclass with an enum identifier and its dependency/anti-dependency metadata, registering it (likely into ENCOUNTER_MAP) at import time.
- Encounter [imported]: Base class defining the encounter interface; subclasses implement async resolve(...). Likely stores name/dependencies/anti-dependencies.
- NullEncounter: No deps/anti-deps. resolve is a no-op. Baseline test event.
- NullEncounterChild: Depends on NULL_EVENT_ENUM. Tests positive dependency resolution (must follow/null be allowed after its parent).
- NullEncounterAntiChild: Anti-depends on NULL_EVENT_ENUM. Tests negative constraint (cannot occur if NULL has occurred).
- OneShotEncounter: Anti-depends on itself (ONESHOT_EVENT_ENUM). Models one-shot behavior (prevent re-triggering).
- FlavorTextEncounter: No deps/anti-deps. Overrides __init__ to set a flavor_text and resolve returns that string (only class that returns a value instead of no-op).

3) Important logic or algorithms
- Decorator-based registration: Each class is immediately registered with metadata when the module is imported, enabling a central registry-driven selection/scheduling mechanism.
- Dependency modeling:
  - Positive dependency: event B requires event A to be present/allowed before it can resolve (NullEncounterChild depends on NULL_EVENT_ENUM).
  - Anti-dependency: event C is barred if event A has occurred or is present (NullEncounterAntiChild).
  - One-shot: implemented by listing the event’s own enum in its anti-dependencies (OneShotEncounter).
- Async resolution: resolve methods are async, indicating integration with an asynchronous engine or loop; most are stubs here.

4) Notable dependencies or patterns
- Registry pattern: Uses ENCOUNTER_MAP (imported; not directly referenced here) and a decorator to register encounter classes keyed by enum identifiers.
- Enum-driven configuration: Event identity and relationships are specified via enums from .enums (imported with a star import).
- Inheritance: All encounters derive from a common Encounter base for consistent interface and metadata handling.
- Minimal side-effect logic: Only FlavorTextEncounter returns content; others are no-ops, underscoring the file’s role as simple/test scaffolding.

=== botc/core/distribution.py ===
1) Purpose of the file
- Defines a simple, table-driven calculator for how many roles of each type (demon, outsider, townsfolk, minion) should be used in a Blood on the Clocktower–style game based on the number of players. It computes counts only; it does not assign roles to players.

2) Key classes/functions
- RoleDistributor
  - Attributes:
    - player_names: the input list of player names (used only to determine player count).
    - num_demons, num_outsiders, num_townsfolk, num_minions: computed integers based on the distribution table.
  - Class constant:
    - DISTRIBUTION_TABLE: maps player counts 5–15 to a 4-tuple (demons, outsiders, townsfolk, minions).
  - Methods:
    - __init__(player_names: List[str]): stores names, computes and sets the four role-count attributes using _calculate_role_counts.
    - _calculate_role_counts(): caps players at 15, special-cases 1 player to 5, and returns the 4-tuple from DISTRIBUTION_TABLE.

3) Important logic or algorithms
- Table-driven role counts:
  - For player counts 5–15, uses fixed tuples that always sum to the player count and always include exactly 1 demon.
- Player count handling:
  - Caps counts at 15 using min(len(player_names), 15).
  - Special-case: if there is exactly 1 player, it treats it as 5 players (returns the 5-player distribution).
  - No handling for 0, 2–4 players: looking up DISTRIBUTION_TABLE will raise a KeyError for these counts (commented-out code suggests a prior approach of auto-filling to 5 players).
- Ordering of tuple values is (demons, outsiders, townsfolk, minions), which is nonstandard relative to common BOTC notation and easy to misread.

4) Notable dependencies or patterns
- Dependencies: only typing.List from the standard library.
- Patterns:
  - Table-driven configuration for business rules (distribution by player count).
  - Initialization computes and stores derived attributes for direct, later consumption.
  - Hard cap at 15 players and a partial fallback for too-few players indicate test-oriented shortcuts and potential edge-case pitfalls.

=== botc/core/__init__.py ===
1) Purpose of the file
- Defines the public API of the botc.core package by re-exporting key classes from internal submodules. This lets consumers import central types via from botc.core import GameManager, RoleDistributor without knowing internal file structure.

2) Key classes/functions (with brief descriptions)
- GameManager (from .game): Re-exported class; presumably the primary coordinator for core game operations. Actual implementation lives in botc/core/game.py.
- RoleDistributor (from .distribution): Re-exported class; presumably handles assigning/distributing roles. Actual implementation lives in botc/core/distribution.py.

3) Important logic or algorithms
- None in this file. It contains only import-forwarding; all behavior and algorithms are in the referenced submodules.

4) Notable dependencies or patterns
- Relative imports: Uses from .game and from .distribution to pull symbols from sibling modules within the package.
- Re-export/“barrel” module pattern: Centralizes exports to provide a stable, concise import surface for users.
- Star-import behavior: Without an __all__ defined, these imported names become part of the module’s globals and will be included in from botc.core import *. Defining __all__ explicitly could make the export surface clearer and more controlled.
- Encapsulation of internal structure: Allows internal file organization to change without breaking external imports, as long as these re-exports remain consistent.

=== botc/core/game.py ===
1) Purpose of the file
- Orchestrates a Discord-enabled game loop for a Blood on the Clocktower–style game.
- Centralizes game state and flow (player roster, day/night cycles, voting, role actions, messaging).
- Provides manager-style abstractions for players, day, night, and Discord I/O; includes support scaffolding for events and board display.

2) Key classes/functions
- EventManager
  - Initializes an encounter Deck (Deck.from_json("default")). Placeholder for future in-game events.
- Counters (dataclass)
  - Tracks turn, day, night integers.
- PlayerManager
  - assign_roles(): Builds the Player list via RoleDistributor.assign_roles() using provided player names.
  - get_players(filter_status=None, filter_role=None, is_registered_role=True): Returns players matching dynamic status predicates and optional role filtering (registered_role or believed_role).
  - get_player(username): Lookup by username.
- DayManager
  - reset_vote_table(): Initializes a voter→candidate 2D vote table using current players.
  - start_voting_phase(interaction): Runs an execution poll via PollManager for users with status.can_vote, with robust error handling (ephemeral fallback).
- NightManager
  - get_wake_order(is_first_night): Computes action order by collecting role_behavior priorities (first night vs other nights) and sorting ascending.
- DiscordManager
  - Wraps Discord components: loads DISCORD_TOKEN (.env), instantiates BotManager, PollManager, GameCommands.
  - send_message(username, content): Direct message via GameCommands.
  - send_query(target, message, choices, max_input): DM poll for role choices/inputs.
  - run_bot(): Starts the bot with token.
- GameManager
  - Core orchestrator: holds managers (player/day/night/discord), counters, game_over flag.
  - get_player/get_players(): Delegates to PlayerManager (note: get_players only exposes registered_role filtering).
  - resolve_temporary_conditions(): Calls status.resolve_temporary_conditions() on all players each cycle.
  - send_query/send_message(): Async wrappers into DiscordManager.
  - message_roles_to_players(): DMs each player their role info (player.show_role()).
  - start_game(interaction): DMs roles; executes first-night actions in wake order; then loops until game_over: increments night counter, resolves temp statuses, runs day voting poll, then runs other-night actions in wake order.
  - get_alive_neighbors(target_player): Returns (left, right) alive neighbors in circular seating, skipping dead players with wrap-around.
  - get_board_str(): Renders a code-block table of seats, players, alive/dead, role/alignment, and status flags (poisoned/protected), with special cases for DRUNK and SPY.

3) Important logic or algorithms
- Dynamic player filtering:
  - Status-based filtering uses: all(getattr(p.status, attr) == required for each filter), enabling flexible queries (e.g., alive, poisoned, can_vote).
  - Role-based filtering optionally on registered_role or believed_role.
- Night wake order:
  - Aggregates players with defined role_behavior priorities (first_night_priority or other_night_priority), sorts numerically to produce the wake sequence.
- Circular neighbor lookup:
  - Computes indexes modulo player count; advances until an alive neighbor is found on each side; breaks if it loops back (edge case can return the target itself if alone alive).
- Discord poll gating and error handling:
  - Voting phase restricted to players with status.can_vote.
  - Catches poll errors; ensures no double-respond on the interaction, falls back to ephemeral error message.
- Temporary condition resolution:
  - Centralized clearing of night-bound or temporary status effects each cycle before day phase voting.
- Board rendering:
  - Uniform tabular string with f-strings; shows role visibility nuances (DRUNK: believed role; SPY: register info).

4) Notable dependencies or patterns
- External libraries:
  - discord.py Interaction-based flow; environment loading via python-dotenv; usage of OS env DISCORD_TOKEN.
- Internal modules:
  - botc.player.Player (with status, role_behavior, take_action, show_role); botc.enums (RoleName et al.); botc.core.distribution.RoleDistributor; botc.encounters.Deck; Discord managers/polling/cogs.
- Architectural patterns:
  - Manager composition: GameManager composes PlayerManager/DayManager/NightManager/DiscordManager and passes a game reference into each (mutual access pattern).
  - Async-first design for Discord I/O and player actions.
  - Data classes and strong typing (PEP 604 unions, e.g., Player|None; bool|int in filters).
- Caveats/observations:
  - Role assignment in start_game() is commented out; roles must be assigned beforehand.
  - GameManager.get_players doesn’t expose is_registered_role toggle; only registered_role filtering is available at this layer.
  - Voting table is initialized but not currently used in start_voting_phase; executed_player isn’t set here.
  - No win-condition checks; game_over is never flipped in this file.
  - Some imports unused (e.g., random, Alignment, RoleClass, Status).

=== botc/behaviors/townsfolk.py ===
1) Purpose of the file
- Implements Townsfolk role behaviors for a Blood on the Clocktower engine, including:
  - Information roles that use a 4-step “data pipeline” (trusted/dishonest defaults + GM overrides).
  - Action roles that target players at night.
  - Custom/hybrid roles that bypass the standard pipelines.
  - Passive/day roles that have no night action.
- Provides a reusable player-filtering helper for targeting and queries.

2) Key classes/functions
- get_filtered_players(game, role_names_registered=None, role_names_believed=None, alignment=None, is_poisoned=None, is_drunk=None, is_alive=None, is_protected=None, excluded_players=None) -> List[Player]
  - Filters game.get_players() by alignment, status flags (poisoned/drunk/alive/protected), role-sets, and exclusions. Used by action/custom roles to build selection lists.

Info roles (4-step pipeline: get_default_trusted, get_manual_trusted, get_default_dishonest, get_manual_dishonest, send_result)
- WasherwomanBehavior (first_night_priority=3)
  - Picks an actual Townsfolk and a decoy (two names, one is the real role) and sends “One of X and Y is the <role>.” Supports GM override of both picks and fake role.
- LibrarianBehavior (first_night_priority=4)
  - Same pattern as Washerwoman, but for Outsiders. Special-case zero-Outsiders: informs “There are no Outsiders in play.” GM override stub returns defaults (comment suggests adding overrides).
- ChefBehavior (first_night_priority=6)
  - Counts adjacent pairs of EVIL players around the table (circular seating). Sends “There are N pairs of evil players.” Dishonest default returns a bounded random count; GM can force a false count.
- UndertakerBehavior (other_night_priority=7)
  - Reveals the role of the player executed during the day: “The player executed today was the <role>.” Fake data picks a random non-actual role; GM can supply a false role.

Action role (targeting pipeline: get_default_target, get_manual_target, execute_action)
- MonkBehavior (other_night_priority=2)
  - Asks the player to choose an alive target to protect; GM may force a target. execute_action sets target.status.protected = True.

Custom/hybrid roles (custom act, bypassing pipelines)
- FortuneTellerBehavior (first_night_priority=8, other_night_priority=9)
  - Prompts the player for two targets. If reliable, returns YES if either is a Demon or marked red_herring; if drunk/poisoned, returns a random YES/NO. Sends “Demon presence: YES/NO”.
- RavenkeeperBehavior (other_night_priority=6)
  - If killed that night, allows selecting one player to learn their role. If reliable, reveals the true registered role; otherwise returns a random different role.

Passive/day roles
- VirginBehavior, SlayerBehavior, SoldierBehavior, MayorBehavior
  - Placeholders; effects triggered in day phase or by other systems.

3) Important logic or algorithms
- Player filtering: get_filtered_players composes multiple criteria into a single pass list filter and supports exclusion sets. Used to ensure targets are valid (e.g., alive, not self).
- Chef adjacency counting: Iterates players in seating order, counts a pair when current and next (with wrap-around) are both EVIL—matching BotC’s “pairs of adjacent evil players” definition.
- Info-role data pipeline: Encodes a standardized flow for truthful vs dishonest info plus optional GM overrides, then a final send_result. Honesty typically governed by player.status.is_reliable (elsewhere).
- Reliability handling:
  - Fortune Teller: Non-reliable results randomize YES/NO; reliable includes red_herring as a positive ping.
  - Ravenkeeper: Non-reliable reveals a random wrong role.
- Undertaker ties into day manager state (game.mgr_day.executed_player) to fetch the executed player’s role and report it at night.

4) Notable dependencies or patterns
- Registration and scheduling:
  - @register_role(RoleName.X) decorator registers behavior classes with role identifiers.
  - first_night_priority and other_night_priority control night action order.
- Core engine API usage:
  - GameManager: get_players, get_player, send_query (interactive prompts to players/GM), send_message, game_master, mgr_day.executed_player, killed_tonight.
  - Player: username, registered_role, registered_alignment, status flags (poisoned, drunk, alive, protected, red_herring, is_reliable).
  - Enums: RoleName (with RoleName.get_by_class), RoleClass (DEMONS, MINIONS, OUTSIDERS, TOWNSFOLK), Alignment.
- Async interaction pattern: All I/O via async send_query/send_message; results often lists of usernames.
- Potential issues/quirks:
  - get_filtered_players: role_names_believed applies to registered_role again (likely intended to filter by a “believed” role attribute).
  - Exclusion uses set subtraction; requires Player to be hashable and may scramble ordering.
  - FortuneTeller allows picking the same target twice; typical rules require two distinct players.
  - Undertaker get_default_trusted returns {"":""} when no executed player or none found; better to return {} consistently and guard for None earlier.
  - Librarian manual overrides are unimplemented (commented suggestion).
  - Chef dishonest default caps at 2; manual permits 0–3.

=== botc/behaviors/base.py ===
1) Purpose of the file
- Defines the base abstractions and archetypes for role behaviors in a Blood on the Clocktower (BoTC) game engine. It standardizes how roles act at night, including:
  - A consistent asynchronous interface for role actions.
  - A GM override mechanism to replace or adjust automated behavior.
  - Template flows for information-giving roles, action-targeting roles, and passive roles.
  - Scheduling hooks via night priority attributes.

2) Key classes/functions
- RoleBehavior (ABC)
  - Properties: first_night_priority, other_night_priority (scheduler hooks), is_reliable (baseline flag; actual reliability taken from player.status.is_reliable).
  - async check_gm_override(game, player) -> bool:
    - Prompts the GM with “Skip Manual Entry … ? Yes/No”.
    - Returns True when the GM chooses to manually intervene (“No” to skipping).
    - Includes role name and user name and annotates “Drunk/Poisoned” state in the prompt.
  - async act(player, game): abstract; implemented by archetypes.

- InfoRoleBehavior(RoleBehavior)
  - For roles that receive info (e.g., Washerwoman, Investigator, Empath, Librarian).
  - async act:
    - Branches on player.status.is_reliable (trusted vs drunk/poisoned).
    - Computes default info via get_default_trusted/get_default_dishonest.
    - If GM wants manual, replaces/adjusts via get_manual_trusted/get_manual_dishonest.
    - Sends results with send_result if data is non-empty.
  - Abstract hooks to implement:
    - get_default_trusted(player, game) -> Dict[str, Any]
    - get_manual_trusted(player, game, default_data) -> Dict[str, Any]
    - get_default_dishonest(player, game) -> Dict[str, Any]
    - get_manual_dishonest(player, game, default_data) -> Dict[str, Any]
    - send_result(player, game, night_data) -> None

- ActionRoleBehavior(RoleBehavior)
  - For roles that target but don’t receive info back (e.g., Monk, Imp, Poisoner).
  - async act:
    - Uses GM override to choose between default vs manual target selection.
    - Executes only if a target is provided.
  - Abstract hooks to implement:
    - get_default_target(player, game) -> Optional[Player]
    - get_manual_target(player, game) -> Optional[Player]
    - execute_action(player, target, game) -> None

- PassiveBehavior(RoleBehavior)
  - For roles that do nothing at night; act is a no-op.

3) Important logic or algorithms
- GM override flow:
  - If no GM (empty string), no override.
  - Prompt is dynamically built with role name, username, and reliability status.
  - “Skip Manual Entry? Yes/No” where selecting “No” means the GM wants to intervene; method returns True in that case.

- Reliability handling:
  - Actual reliability is taken from player.status.is_reliable (e.g., drunk/poisoned); this toggles trusted vs dishonest info paths in InfoRoleBehavior.

- Template method pattern in act():
  - Info roles: default compute → optional manual adjustment → send results.
  - Action roles: choose target (default or manual) → execute action if valid.
  - Passive roles: do nothing.

- Scheduling hooks:
  - first_night_priority and other_night_priority are present for an external scheduler to order actions across nights.

4) Notable dependencies or patterns
- Async I/O throughout: all actions and prompts are async to fit an event loop/game loop.
- Dependencies (TYPE_CHECKING to avoid import cycles):
  - GameManager: must provide game_master (str) and send_query(user_id, text, options, max_choices) -> list/selection or None.
  - Player: must provide username, registered_role (stringifiable), and status.is_reliable.
- Design patterns:
  - Strategy/Template Method via abstract hooks that concrete roles implement while inheriting shared act() flow.
  - Separation of automated defaults vs manual GM overrides to support both fully-automated and storyteller-driven games.
- Minor notes:
  - RoleName and register_role are imported but not used in this file (likely used elsewhere for role registration/typing).
  - str(player.registered_role) implies roles or role enums implement a meaningful __str__.
  - is_reliable on RoleBehavior is a baseline attribute; the code’s operative flag is player.status.is_reliable.

=== botc/behaviors/demons.py ===
1) Purpose of the file
- Implements the behavior for the Imp (a Demon role in Blood on the Clocktower) during night phases. It defines how the Imp selects a victim, applies protection checks, and records the night kill within the game state.

2) Key classes/functions
- ImpBehavior(RoleBehavior)
  - other_night_priority = 5: Scheduling hint for action order on non-first nights.
  - async act(player, game): Prompts the Imp to choose a single living target (excluding self) via a Discord dropdown UI, applies protection logic, marks the target as killed for the night, and updates game state.

- register_role(RoleName.IMP) decorator
  - Registers ImpBehavior with the role registry so the engine can resolve the correct behavior for the IMP role.

3) Important logic or algorithms
- Target selection:
  - Builds a list of valid targets as usernames of living players excluding the acting Imp.
  - Uses game.send_query to asynchronously present a single-select dropdown to the acting player.
- Kill resolution:
  - If no selection, the Imp does nothing.
  - Retrieves the selected Player by username; if target exists and is not protected, sets target.status.alive = False and records game.killed_tonight = target.
  - If protected (status.is_protected), the kill is prevented and a message indicates Monk protection.
- Night flow integration:
  - other_night_priority guides when the Imp acts relative to other roles on later nights.
  - Stores the kill in game.killed_tonight for downstream dawn resolution/announcements.

4) Notable dependencies or patterns
- Registration pattern: register_role maps RoleName.IMP to ImpBehavior, enabling a plugin-like role behavior registry.
- Async interaction/UI: act is async and relies on game.send_query to interface with a Discord dropdown instead of terminal input, decoupling game logic from UI.
- GameManager/Player API usage: game.get_players(), game.get_player(), game.killed_tonight indicate centralized state management and player lookup through the GameManager.
- Status modeling: Uses Player.status fields alive and is_protected, enabling cross-role interactions (e.g., Monk protection).
- Type handling: from __future__ import annotations and TYPE_CHECKING imports prevent runtime circular imports and speed up module load while preserving type hints.
- Debugging: Uses print statements for logging (could be replaced by a logger elsewhere in the codebase).

=== botc/behaviors/minions.py ===
1) Purpose of the file
- Defines the in-game night behaviors for the minion roles (Poisoner, Spy, Scarlet Woman) in a Blood on the Clocktower-like engine.
- Assigns action priorities for first and subsequent nights, and implements the Scarlet Woman’s role-morphing into the Imp when conditions are met.

2) Key classes/functions
- register_role(RoleName): Decorator that registers a RoleBehavior subclass under a RoleName in a global behavior map.
- PoisonerBehavior(RoleBehavior)
  - first_night_priority = 1; other_night_priority = 1
  - act: Prompts the Poisoner to select exactly one alive player (by username). If a choice is made, sets target.status.poisoned = True. Sends the current board string to the acting player.
- SpyBehavior(RoleBehavior)
  - first_night_priority = 2; other_night_priority = 3
  - act: Sends the current board string (presumably revealing tokens/info) to the Spy each night.
- ScarletWomanBehavior(RoleBehavior)
  - other_night_priority = 4 (no first-night action)
  - act: Locates the Imp and counts alive players. If the Imp is dead and there are 5+ players alive, converts the Scarlet Woman into the Imp by updating:
    - player.registered_role and player.believed_role to RoleName.IMP
    - player.registered_alignment to Alignment.EVIL
    - player.role_behavior to BEHAVIOR_MAP[RoleName.IMP]
    - Notifies the player of the change.

3) Important logic or algorithms
- Night action ordering via first_night_priority and other_night_priority ensures Poisoner acts before Spy, and Scarlet Woman’s conversion check occurs later in the night.
- Asynchronous interaction model:
  - send_query returns a list of selected option strings or None on timeout; code guards against None.
  - send_message used to deliver private info (e.g., board state to Spy, confirmation/info to Poisoner/Scarlet Woman).
- Selection mechanics:
  - Poisoner chooses from alive players only; options passed as strings (usernames), matching send_query’s requirements.
- Status effect application:
  - Poisoning is modeled as a boolean flag (status.poisoned) on the target; clearing/expiration is handled elsewhere in the engine.
- Role morphing:
  - Scarlet Woman conversion gate matches the standard rule (Imp dead and 5+ alive). Conversion updates both declared and believed role, alignment, and swaps in the new behavior so future nights use Imp logic.

4) Notable dependencies or patterns
- Enums: RoleName and Alignment drive role identification and team alignment.
- GameManager interface usage:
  - get_players with filter dicts (e.g., {"alive": True}) and role filters (filter_roles=[RoleName.IMP])
  - get_player(username) lookup
  - send_query(user, prompt, options, n_choices)
  - send_message(user, text)
  - get_board_str() snapshot used for private info sharing
- Player model expectations:
  - username, believed_role, registered_role, registered_alignment, role_behavior
  - status fields: alive, poisoned
- Registration pattern:
  - register_role + BEHAVIOR_MAP centralize behavior lookup by role; enables dynamic behavior swapping (Scarlet Woman -> Imp).
- Typing/structure:
  - from __future__ import annotations and TYPE_CHECKING to avoid runtime import cycles.
- Notes/pitfalls:
  - get_players(filter_roles=[RoleName.IMP])[0] assumes an Imp exists; could IndexError if none.
  - Poisoner logs “Put {player.believed_role} to sleep.” (misleading text/subject) via print, not user-facing.
  - Poisoner receiving the board string may be intentional or overly informative depending on game design.

=== botc/behaviors/__init__.py ===
1) Purpose of the file
- Centralizes a registry mapping role names to behavior instances for the package’s behavior system.
- Provides a decorator to auto-register behavior classes to one or more role names at import time.
- Eagerly imports behavior submodules so their decorated classes are instantiated and registered as soon as botc.behaviors is imported.

2) Key classes/functions
- BEHAVIOR_MAP: Global dict mapping role_name -> behavior_instance. Acts as the service locator/registry for behaviors.
- register_role(*role_names): Decorator factory that:
  - Instantiates the decorated class with no arguments (cls()).
  - Registers that single instance under each provided role name in BEHAVIOR_MAP.
  - Returns the original class unchanged (side-effect-only decorator).
- Imports: from . import base, demons, minions, outsiders, townsfolk to trigger module import side-effects (i.e., execute decorators that populate BEHAVIOR_MAP).

3) Important logic or algorithms
- Registration via decorator at import time:
  - Eager and side-effect driven: importing botc.behaviors pulls in submodules, which run their class definitions and decorators to populate BEHAVIOR_MAP.
- Single instance per behavior class:
  - The same instance is reused for all role names provided to the decorator; implies behaviors should be stateless or carefully manage shared state.
- Silent overwrite behavior:
  - If multiple classes register the same role name, the last one imported wins with no warning or error.
- Zero-arg requirement:
  - Decorated behavior classes must have a no-argument constructor, as instantiation is hard-coded to cls().
- Global mutable registry:
  - Consumers must ensure registration has occurred (i.e., botc.behaviors imported) before lookup.
  - Not designed for concurrent mutation; relies on Python’s typically single-threaded import execution.

4) Notable dependencies or patterns
- Patterns:
  - Decorator-based plugin/registry pattern.
  - Service locator via a module-level dict.
  - Import side-effects to auto-discover/register implementations (static list of submodules rather than dynamic discovery).
- Dependencies:
  - Pure Python; only intra-package relative imports (.base, .demons, .minions, .outsiders, .townsfolk).
- Architectural considerations:
  - Potential circular import sensitivity is mitigated by defining register_role before importing submodules.
  - Requires updating this __init__ to include new submodules to participate in auto-registration.
  - Role names are free-form strings; no validation/enum guarantees, so typos or collisions go unnoticed.

=== botc/behaviors/outsiders.py ===
1) Purpose of the file
- Defines behavior implementations for Outsider roles in the game (Blood on the Clocktower). Currently implements the Butler’s night action, including its scheduling priority and the private message sent to the player.

2) Key classes/functions
- @register_role(RoleName.BUTLER): Decorator that registers the behavior class with the role registry so the engine can look up actions by role.
- ButlerBehavior(RoleBehavior):
  - first_night_priority = 9, other_night_priority = 10: Class-level attributes used by the game engine to order night actions.
  - async def act(self, player: Player, game: GameManager): Picks a random player as the Butler’s “master” and privately messages the Butler to “worship” them.

3) Important logic or algorithms
- Random target selection: master = random.sample(game.get_players(), 1)[0] selects a random player (including possibly the Butler themself) as the “master.”
- Asynchronous action: act is an async coroutine that awaits game.send_message, fitting into an async night phase system.
- Night-order semantics: Distinct first-night vs. other-night priorities enable correct sequencing with other roles.

4) Notable dependencies or patterns
- RoleBehavior base class: Establishes the contract for role behaviors (act method, priority attributes).
- RoleName enum: Identifies the role (BUTLER) for registration/lookup.
- Plugin/registry pattern: register_role decorator suggests a modular, discoverable set of role behaviors.
- Type hinting patterns: from __future__ import annotations and TYPE_CHECKING-guarded imports to avoid runtime circular dependencies and reduce import overhead.
- Uses GameManager interfaces: Expects game.get_players() and game.send_message(recipient_username, text).
- Non-determinism: Relies on Python’s random module; no seeding here, so behavior varies per run.

=== botc/discord_manager/cogs/__init__.py ===
I don’t have the contents of botc/discord_manager/cogs/__init__.py to analyze. Please paste the file’s code (and, if relevant, a quick tree of botc/discord_manager/cogs) so I can produce the structured summary you requested.

If the file is empty or only defines package exports, say so and I’ll still summarize its role (e.g., marking the cogs package, re-exporting submodules, or providing a setup hook).

=== botc/discord_manager/cogs/game_commands.py ===
1) Purpose of the file
- Defines a Discord Cog (GameCommands) that provides admin and utility commands to run and manage a “Blood on the Clocktower” game in Discord.
- Orchestrates lobby creation, seat ordering, GM selection, game start, and visibility of current game state.
- Provides DM utilities (dropdowns and polls) for private player selections.

2) Key classes/functions
- GameCommands(commands.Cog)
  - __init__(bot, game): Stores BotManager and GameManager references.
  - set_seats(interaction, mentions): Admin slash command. Parses a string of user mentions to set/lock the seating order. Validates count and membership against the active roster, reorders mgr_player.player_list and mgr_player.player_names, and shows the updated grimoire.
  - dmdropdown(user_name, message_text, options, max_selection): Sends a DM containing a custom DropdownView to a user resolved by name/global_name from bot cache. Deduplicates options (<= 25), waits for selection or timeout, returns selected values or None.
  - dmpoll(user_name, poll_message, poll_options, max_selection): Sends a DM poll using discord.Poll when <= 10 options and max_selection > 1; otherwise falls back to dmdropdown. Polls the message repeatedly to detect selections, enforces max_selection, ends the poll, and returns chosen options.
  - send_direct_message(user_name, message): Utility to DM a user (by name/global_name lookup in cache). Returns success boolean with console logging.
  - print_users(ctx): Text command (manage_roles). Prints all guild members’ IDs, names, global names, and roles to terminal; replies with a count.
  - open_lobby(interaction): Admin slash command. Clears current player_names, posts an embed with a JoinLobbyView for players to join.
  - start_game(interaction): Admin slash command. Closes lobby, runs a GM selection poll via PollManager, then (test code) injects many placeholder Player entries across role classes into mgr_player.player_list before calling game.start_game.
  - display_grimoire(interaction): Slash command. Sends the current board/grimoire string ephemerally.
  - display_players(interaction): Slash command. Shows current joined players; warns ephemerally if none.

3) Important logic or algorithms
- Seating order from mentions:
  - Extracts user IDs from a free-form mention string via regex (<@!?…>), resolves users to names, and checks the list matches the active roster (by name).
  - Reorders the internal player list to match the provided order and updates related names list.
  - Note: Name comparisons mix lowercase-normalized current roster names with case-sensitive new names, which can cause false mismatches if casing differs.
- DM selection mechanics:
  - Dropdown path: Deduplicates options, enforces max 25, sends a custom view, waits on view, disables the select on timeout, and returns chosen values.
  - Poll path: Uses discord.Poll in DMs (duration 1h, multiple selection when allowed). Periodically refetches the message to read answer vote_counts. If too many choices are selected, ends the poll, warns the user, and restarts the flow. Ends the poll once valid selections are made and returns selected option texts.
  - Selection type fallback: If options > 10 or single-selection, it automatically uses the dropdown view instead of poll.
- GM selection and test data injection:
  - PollManager.run_gamemaster_poll handles GM choice; after that, the function appends a large set of test players built from RoleName.get_by_class across role classes (including repeated username "iiiii5184" and Test1-3) before starting the game. This is explicitly marked as testing.
- User resolution:
  - Multiple functions resolve users by matching provided username/global_name (case-insensitive) against the bot’s user cache only; no fallback to fetch by ID in DM utilities.

4) Notable dependencies or patterns
- Discord API usage:
  - discord.ext.commands Cog and classic text command.
  - discord.app_commands slash commands with default_permissions and ephemeral interaction responses.
  - discord.ui Views (JoinLobbyView, DropdownView) with expected attributes selected_values and a select component the code can disable.
  - discord.Poll (newer API) for DM polls; manual polling loop via channel.fetch_message to read live poll state.
- Internal game model integration:
  - GameManager: get_players, get_board_str, start_game; direct mutation of mgr_player.player_list and mgr_player.player_names.
  - Player, Alignment; enums RoleName and RoleClass for role groupings.
  - PollManager for GM selection.
- Patterns and caveats:
  - Direct state mutation of game manager lists rather than via dedicated methods.
  - Heavy reliance on username/global_name string matching and bot user cache; no robust ID-based mapping for DM utilities (potential mismatches/ambiguity if duplicate names).
  - Regex parsing of mentions string in a slash command rather than structured mention inputs.
  - Ephemeral flag used correctly for interactions, but also passed to ctx.send in a text command (not supported in standard discord.py).
  - Test scaffolding code left in start_game that auto-injects many players/roles.

